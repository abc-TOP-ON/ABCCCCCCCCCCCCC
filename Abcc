-- LYNEX - نسخة مبسطة ومضمونة
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

-- حذف الواجهة القديمة
if LocalPlayer.PlayerGui:FindFirstChild("LYNEX") then
    LocalPlayer.PlayerGui:FindFirstChild("LYNEX"):Destroy()
end

-- إنشاء الواجهة
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LYNEX"
ScreenGui.Parent = LocalPlayer.PlayerGui
ScreenGui.ResetOnSpawn = false

-- زر فتح/إغلاق
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 60, 0, 60)
ToggleButton.Position = UDim2.new(0.05, 0, 0.5, -30)
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
ToggleButton.Text = "LYNEX"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 16
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = ScreenGui
ToggleButton.ZIndex = 100

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(1, 0)
ButtonCorner.Parent = ToggleButton

-- الواجهة الرئيسية
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
MainFrame.Visible = false
MainFrame.ZIndex = 2

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

-- عنوان
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Text = "🎮 LYNEX"
Title.TextColor3 = Color3.fromRGB(0, 200, 255)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame
Title.ZIndex = 3

-- زر إغلاق
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -45, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 20
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame
CloseBtn.ZIndex = 3

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn

-- ============================================
-- إعدادات الاعب (سرعة + قفز)
-- ============================================

-- إطار السرعة
local SpeedFrame = Instance.new("Frame")
SpeedFrame.Size = UDim2.new(0.9, 0, 0.25, 0)
SpeedFrame.Position = UDim2.new(0.05, 0, 0.15, 0)
SpeedFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
SpeedFrame.BorderSizePixel = 0
SpeedFrame.Parent = MainFrame
SpeedFrame.ZIndex = 3

local SpeedFrameCorner = Instance.new("UICorner")
SpeedFrameCorner.CornerRadius = UDim.new(0, 8)
SpeedFrameCorner.Parent = SpeedFrame

local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Size = UDim2.new(0.6, 0, 1, 0)
SpeedLabel.Position = UDim2.new(0.05, 0, 0, 0)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "🏃 سرعة الاعب:"
SpeedLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
SpeedLabel.TextSize = 16
SpeedLabel.Font = Enum.Font.GothamBold
SpeedLabel.TextXAlignment = Enum.TextXAlignment.Left
SpeedLabel.Parent = SpeedFrame
SpeedLabel.ZIndex = 4

local SpeedInput = Instance.new("TextBox")
SpeedInput.Size = UDim2.new(0.25, 0, 0.7, 0)
SpeedInput.Position = UDim2.new(0.7, 0, 0.15, 0)
SpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedInput.TextSize = 18
SpeedInput.Font = Enum.Font.GothamBold
SpeedInput.Text = "16"
SpeedInput.Parent = SpeedFrame
SpeedInput.ZIndex = 4

local SpeedInputCorner = Instance.new("UICorner")
SpeedInputCorner.CornerRadius = UDim.new(0, 6)
SpeedInputCorner.Parent = SpeedInput

SpeedInput.FocusLost:Connect(function()
    local val = tonumber(SpeedInput.Text)
    if val and val > 0 then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.WalkSpeed = val
        end
    end
end)

-- إطار القفز
local JumpFrame = Instance.new("Frame")
JumpFrame.Size = UDim2.new(0.9, 0, 0.25, 0)
JumpFrame.Position = UDim2.new(0.05, 0, 0.45, 0)
JumpFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
JumpFrame.BorderSizePixel = 0
JumpFrame.Parent = MainFrame
JumpFrame.ZIndex = 3

local JumpFrameCorner = Instance.new("UICorner")
JumpFrameCorner.CornerRadius = UDim.new(0, 8)
JumpFrameCorner.Parent = JumpFrame

local JumpLabel = Instance.new("TextLabel")
JumpLabel.Size = UDim2.new(0.6, 0, 1, 0)
JumpLabel.Position = UDim2.new(0.05, 0, 0, 0)
JumpLabel.BackgroundTransparency = 1
JumpLabel.Text = "🦘 قوة القفز:"
JumpLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
JumpLabel.TextSize = 16
JumpLabel.Font = Enum.Font.GothamBold
JumpLabel.TextXAlignment = Enum.TextXAlignment.Left
JumpLabel.Parent = JumpFrame
JumpLabel.ZIndex = 4

local JumpInput = Instance.new("TextBox")
JumpInput.Size = UDim2.new(0.25, 0, 0.7, 0)
JumpInput.Position = UDim2.new(0.7, 0, 0.15, 0)
JumpInput.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
JumpInput.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpInput.TextSize = 18
JumpInput.Font = Enum.Font.GothamBold
JumpInput.Text = "50"
JumpInput.Parent = JumpFrame
JumpInput.ZIndex = 4

local JumpInputCorner = Instance.new("UICorner")
JumpInputCorner.CornerRadius = UDim.new(0, 6)
JumpInputCorner.Parent = JumpInput

JumpInput.FocusLost:Connect(function()
    local val = tonumber(JumpInput.Text)
    if val and val > 0 then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.JumpPower = val
        end
    end
end)

-- عرض القيم الحالية
local CurrentValues = Instance.new("TextLabel")
CurrentValues.Size = UDim2.new(0.9, 0, 0.15, 0)
CurrentValues.Position = UDim2.new(0.05, 0, 0.78, 0)
CurrentValues.BackgroundColor3 = Color3.fromRGB(15, 15, 30)
CurrentValues.TextColor3 = Color3.fromRGB(180, 180, 210)
CurrentValues.TextSize = 14
CurrentValues.Font = Enum.Font.GothamMedium
CurrentValues.Text = "📊 السرعة: 16 | القفز: 50"
CurrentValues.Parent = MainFrame
CurrentValues.ZIndex = 3

local CurrentCorner = Instance.new("UICorner")
CurrentCorner.CornerRadius = UDim.new(0, 8)
CurrentCorner.Parent = CurrentValues

-- تحديث القيم
task.spawn(function()
    while ScreenGui do
        task.wait(0.5)
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            local hum = char.Humanoid
            CurrentValues.Text = "📊 السرعة: " .. math.round(hum.WalkSpeed) .. " | القفز: " .. math.round(hum.JumpPower)
        end
    end
end)

-- ============================================
-- أحداث الأزرار
-- ============================================

local isOpen = false

ToggleButton.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
end)

CloseBtn.MouseButton1Click:Connect(function()
    isOpen = false
    MainFrame.Visible = false
end)

-- جعل النافذة قابلة للسحب
local dragToggle = false
local dragInput2, dragStart2, startPos2

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragToggle = true
        dragStart2 = input.Position
        startPos2 = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragToggle = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        if dragToggle then
            local delta = input.Position - dragStart2
            MainFrame.Position = UDim2.new(
                startPos2.X.Scale,
                startPos2.X.Offset + delta.X,
                startPos2.Y.Scale,
                startPos2.Y.Offset + delta.Y
            )
        end
    end
end)

print("✅ LYNEX شغال 100%!")
print("🎮 إعدادات الاعب: سرعة + قفز")
