-- 7MO HUB v15 - نسخة الهاتف (مع سكربت تجميع الفلوس الفوري + الصيد التلقائي + الأخبار + المعلومات)
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserId = LocalPlayer.UserId
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local TweenService = game:GetService("TweenService")

-- منع تكرار الواجهة
if game:GetService("CoreGui"):FindFirstChild("7MOHUBv15") then
    game:GetService("CoreGui"):FindFirstChild("7MOHUBv15"):Destroy()
end

-- المتغيرات العامة
local isOpen = false
local buttonSize = 60

-- ============================================
-- زر الدخول والخروج الدائري
-- ============================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "7MOHUBv15"
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

-- الزر الدائري الرئيسي
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, buttonSize, 0, buttonSize)
ToggleButton.Position = UDim2.new(0.05, 0, 0.5, -buttonSize/2)
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleButton.BackgroundTransparency = 0
ToggleButton.Image = "rbxassetid://0"
ToggleButton.Parent = ScreenGui
ToggleButton.ZIndex = 100

-- جعل الزر دائري
local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(1, 0)
ButtonCorner.Parent = ToggleButton

-- حلقة متحركة (Glow Ring)
local GlowRing = Instance.new("ImageLabel")
GlowRing.Name = "GlowRing"
GlowRing.Size = UDim2.new(1.3, 0, 1.3, 0)
GlowRing.Position = UDim2.new(-0.15, 0, -0.15, 0)
GlowRing.BackgroundTransparency = 1
GlowRing.Image = "rbxassetid://0"
GlowRing.Parent = ToggleButton
GlowRing.ZIndex = 99

-- إطار النيون المتحرك
local NeonRing = Instance.new("Frame")
NeonRing.Name = "NeonRing"
NeonRing.Size = UDim2.new(1.4, 0, 1.4, 0)
NeonRing.Position = UDim2.new(-0.2, 0, -0.2, 0)
NeonRing.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
NeonRing.BackgroundTransparency = 0.3
NeonRing.BorderSizePixel = 3
NeonRing.BorderColor3 = Color3.fromRGB(180, 0, 255)
NeonRing.Parent = ToggleButton
NeonRing.ZIndex = 98

local NeonCorner = Instance.new("UICorner")
NeonCorner.CornerRadius = UDim.new(1, 0)
NeonCorner.Parent = NeonRing

-- حلقة متحركة ثانية (تأثير النبض)
local PulseRing = Instance.new("Frame")
PulseRing.Name = "PulseRing"
PulseRing.Size = UDim2.new(1.5, 0, 1.5, 0)
PulseRing.Position = UDim2.new(-0.25, 0, -0.25, 0)
PulseRing.BackgroundColor3 = Color3.fromRGB(150, 0, 255)
PulseRing.BackgroundTransparency = 0.5
PulseRing.BorderSizePixel = 0
PulseRing.Parent = ToggleButton
PulseRing.ZIndex = 97

local PulseCorner = Instance.new("UICorner")
PulseCorner.CornerRadius = UDim.new(1, 0)
PulseCorner.Parent = PulseRing

-- النص داخل الزر
local ButtonText = Instance.new("TextLabel")
ButtonText.Name = "ButtonText"
ButtonText.Size = UDim2.new(1, 0, 1, 0)
ButtonText.Position = UDim2.new(0, 0, 0, 0)
ButtonText.BackgroundTransparency = 1
ButtonText.Text = "7MO"
ButtonText.TextColor3 = Color3.fromRGB(255, 255, 255)
ButtonText.TextSize = 22
ButtonText.Font = Enum.Font.GothamBold
ButtonText.TextScaled = true
ButtonText.Parent = ToggleButton
ButtonText.ZIndex = 101

-- جعل الزر قابل للسحب
local dragging = false
local dragInput, dragStart, startPos

ToggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = ToggleButton.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

ToggleButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragging then
            local delta = input.Position - dragStart
            ToggleButton.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end
end)

-- تأثيرات الحركة على الحلقات
local ringColors = {
    Color3.fromRGB(120, 0, 255),
    Color3.fromRGB(180, 0, 255),
    Color3.fromRGB(220, 0, 255),
    Color3.fromRGB(180, 0, 255),
    Color3.fromRGB(120, 0, 255)
}

local colorIndex = 1

-- حلقة التحديث المستمر للنيون
task.spawn(function()
    while ScreenGui and ToggleButton.Parent do
        -- تحديث ألوان الحلقات
        colorIndex = colorIndex + 0.02
        local index = math.floor(colorIndex) % #ringColors + 1
        local nextIndex = index % #ringColors + 1
        local alpha = colorIndex % 1
        
        local currentColor = ringColors[index]
        local nextColor = ringColors[nextIndex]
        
        local r = currentColor.R + (nextColor.R - currentColor.R) * alpha
        local g = currentColor.G + (nextColor.G - currentColor.G) * alpha
        local b = currentColor.B + (nextColor.B - currentColor.B) * alpha
        
        local finalColor = Color3.new(r, g, b)
        
        -- تحديث لون النيون
        NeonRing.BackgroundColor3 = finalColor
        NeonRing.BorderColor3 = finalColor
        
        -- نبض الشفافية
        local breathe = 0.2 + math.sin(tick() * 2.5) * 0.2
        NeonRing.BackgroundTransparency = breathe
        PulseRing.BackgroundTransparency = breathe + 0.2
        
        -- تغيير حجم الحلقة النابضة
        local pulseSize = 1.5 + math.sin(tick() * 2) * 0.1
        PulseRing.Size = UDim2.new(pulseSize, 0, pulseSize, 0)
        PulseRing.Position = UDim2.new(-(pulseSize-1)/2, 0, -(pulseSize-1)/2, 0)
        
        task.wait(0.03)
    end
end)

-- ============================================
-- النافذة الرئيسية (تظهر عند الضغط على الزر)
-- ============================================

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 520, 0, 480)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -240)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
MainFrame.ZIndex = 2
MainFrame.Visible = false

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

-- الإطار البنفسجي
local GlowFrame = Instance.new("Frame")
GlowFrame.Name = "GlowFrame"
GlowFrame.Size = UDim2.new(1, 8, 1, 8)
GlowFrame.Position = UDim2.new(0, -4, 0, -4)
GlowFrame.BackgroundColor3 = Color3.fromRGB(120, 0, 255)
GlowFrame.BackgroundTransparency = 0.3
GlowFrame.BorderSizePixel = 0
GlowFrame.Parent = MainFrame
GlowFrame.ZIndex = 0

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(0, 16)
GlowCorner.Parent = GlowFrame

local GlowFrame2 = Instance.new("Frame")
GlowFrame2.Name = "GlowFrame2"
GlowFrame2.Size = UDim2.new(1, 14, 1, 14)
GlowFrame2.Position = UDim2.new(0, -7, 0, -7)
GlowFrame2.BackgroundColor3 = Color3.fromRGB(150, 0, 255)
GlowFrame2.BackgroundTransparency = 0.15
GlowFrame2.BorderSizePixel = 0
GlowFrame2.Parent = MainFrame
GlowFrame2.ZIndex = -1

local GlowCorner2 = Instance.new("UICorner")
GlowCorner2.CornerRadius = UDim.new(0, 20)
GlowCorner2.Parent = GlowFrame2

-- تحريك الإطار البنفسجي
local glowColors2 = {
    Color3.fromRGB(120, 0, 255),
    Color3.fromRGB(180, 0, 255),
    Color3.fromRGB(220, 0, 255),
    Color3.fromRGB(180, 0, 255),
    Color3.fromRGB(120, 0, 255)
}

local colorIndex2 = 1
RunService.Heartbeat:Connect(function()
    if not MainFrame.Visible then return end
    colorIndex2 = colorIndex2 + 0.02
    local index = math.floor(colorIndex2) % #glowColors2 + 1
    local nextIndex = index % #glowColors2 + 1
    local alpha = colorIndex2 % 1
    
    local currentColor = glowColors2[index]
    local nextColor = glowColors2[nextIndex]
    
    local r = currentColor.R + (nextColor.R - currentColor.R) * alpha
    local g = currentColor.G + (nextColor.G - currentColor.G) * alpha
    local b = currentColor.B + (nextColor.B - currentColor.B) * alpha
    
    GlowFrame.BackgroundColor3 = Color3.new(r, g, b)
    GlowFrame2.BackgroundColor3 = Color3.new(r + 0.1, g + 0.1, b + 0.1)
    
    local breathe = 0.2 + math.sin(tick() * 2) * 0.15
    GlowFrame.BackgroundTransparency = breathe
    GlowFrame2.BackgroundTransparency = breathe - 0.1
end)

-- زر الإغلاق X (داخل النافذة)
local CloseX = Instance.new("TextButton")
CloseX.Name = "CloseX"
CloseX.Size = UDim2.new(0, 35, 0, 35)
CloseX.Position = UDim2.new(1, -45, 0, 8)
CloseX.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
CloseX.BorderSizePixel = 0
CloseX.Text = "✕"
CloseX.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseX.TextSize = 20
CloseX.Font = Enum.Font.GothamBold
CloseX.Parent = MainFrame
CloseX.ZIndex = 20

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseX

CloseX.MouseEnter:Connect(function()
    CloseX.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
end)

CloseX.MouseLeave:Connect(function()
    CloseX.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
end)

CloseX.MouseButton1Click:Connect(function()
    toggleUI()
end)

-- ============================================
-- وظيفة إظهار/إخفاء الواجهة
-- ============================================

function toggleUI()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
    
    if isOpen then
        -- تغيير لون الزر إلى بنفسجي
        ToggleButton.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
        ButtonText.TextColor3 = Color3.fromRGB(255, 255, 255)
        -- تغيير الحجم قليلاً
        local tween = TweenService:Create(ToggleButton, TweenInfo.new(0.2), {Size = UDim2.new(0, buttonSize * 1.1, 0, buttonSize * 1.1)})
        tween:Play()
        task.wait(0.2)
        local tween2 = TweenService:Create(ToggleButton, TweenInfo.new(0.1), {Size = UDim2.new(0, buttonSize, 0, buttonSize)})
        tween2:Play()
    else
        -- إعادة اللون إلى الأسود
        ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        ButtonText.TextColor3 = Color3.fromRGB(255, 255, 255)
        local tween = TweenService:Create(ToggleButton, TweenInfo.new(0.2), {Size = UDim2.new(0, buttonSize * 0.9, 0, buttonSize * 0.9)})
        tween:Play()
        task.wait(0.2)
        local tween2 = TweenService:Create(ToggleButton, TweenInfo.new(0.1), {Size = UDim2.new(0, buttonSize, 0, buttonSize)})
        tween2:Play()
    end
end

-- ربط زر الدخول/الخروج
ToggleButton.MouseButton1Click:Connect(function()
    if not dragging then
        toggleUI()
    end
end)

-- ============================================
-- القائمة الجانبية
-- ============================================

local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0, 140, 1, 0)
Sidebar.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame
Sidebar.ZIndex = 15

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 12)
SidebarCorner.Parent = Sidebar

local SidebarFix = Instance.new("Frame")
SidebarFix.Size = UDim2.new(0, 20, 1, 0)
SidebarFix.Position = UDim2.new(1, -20, 0, 0)
SidebarFix.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
SidebarFix.BorderSizePixel = 0
SidebarFix.Parent = Sidebar
SidebarFix.ZIndex = 15

-- شعار 7MO HUB v15
local LogoText = Instance.new("TextLabel")
LogoText.Size = UDim2.new(1, 0, 0, 50)
LogoText.BackgroundTransparency = 1
LogoText.Text = "7MO HUB v15"
LogoText.TextColor3 = Color3.fromRGB(180, 0, 255)
LogoText.TextSize = 15
LogoText.Font = Enum.Font.GothamBold
LogoText.Parent = Sidebar
LogoText.ZIndex = 16

task.spawn(function()
    while ScreenGui do
        local brightness = 0.5 + math.sin(tick() * 1.5) * 0.5
        LogoText.TextColor3 = Color3.new(0.5 + brightness * 0.2, 0, 0.7 + brightness * 0.3)
        task.wait(0.05)
    end
end)

-- أزرار القائمة الجانبية (مع إضافة زر المعلومات)
local function createSidebarButton(text, posIndex, isActive)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -20, 0, 40)
    Btn.Position = UDim2.new(0, 10, 0, 60 + ((posIndex - 1) * 48))
    
    if isActive then
        Btn.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
        Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
        Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
    
    Btn.BorderSizePixel = 0
    Btn.Text = text
    Btn.TextSize = 14
    Btn.Font = Enum.Font.GothamBold
    Btn.Parent = Sidebar
    Btn.ZIndex = 17

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 8)
    BtnCorner.Parent = Btn
    
    Btn.MouseEnter:Connect(function()
        if not isActive then
            Btn.BackgroundColor3 = Color3.fromRGB(70, 70, 95)
        end
    end)
    Btn.MouseLeave:Connect(function()
        if not isActive then
            Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
        end
    end)
    
    if isActive then
        local Indicator = Instance.new("Frame")
        Indicator.Size = UDim2.new(0, 4, 1, -12)
        Indicator.Position = UDim2.new(0, 4, 0, 6)
        Indicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        Indicator.BorderSizePixel = 0
        Indicator.Parent = Btn
        Indicator.ZIndex = 18
    end
end

-- إنشاء الأزرار (مع زر المعلومات الجديد)
createSidebarButton("🏠 الرئيسية", 1, true)
createSidebarButton("🎉 ترحيب", 2, false)
createSidebarButton("⚡ انتقال الفوري", 3, false)
createSidebarButton("💰 فلوس", 4, false)
createSidebarButton("🎣 الصيد", 5, false)
createSidebarButton("👥 لاعبين", 6, false)
createSidebarButton("📰 الأخبار", 7, false)
createSidebarButton("📋 المعلومات", 8, false)
createSidebarButton("⚙️ لاعب", 9, false)

-- حاويات المحتوى
local HomeContainer = Instance.new("Frame")
HomeContainer.Name = "HomeContainer"
HomeContainer.Size = UDim2.new(1, -150, 1, -20)
HomeContainer.Position = UDim2.new(0, 150, 0, 10)
HomeContainer.BackgroundTransparency = 1
HomeContainer.Parent = MainFrame
HomeContainer.ZIndex = 1
HomeContainer.Visible = true

local WelcomeContainer = Instance.new("Frame")
WelcomeContainer.Name = "WelcomeContainer"
WelcomeContainer.Size = UDim2.new(1, -150, 1, -20)
WelcomeContainer.Position = UDim2.new(0, 150, 0, 10)
WelcomeContainer.BackgroundTransparency = 1
WelcomeContainer.Parent = MainFrame
WelcomeContainer.ZIndex = 1
WelcomeContainer.Visible = false

local TeleportContainer = Instance.new("Frame")
TeleportContainer.Name = "TeleportContainer"
TeleportContainer.Size = UDim2.new(1, -150, 1, -20)
TeleportContainer.Position = UDim2.new(0, 150, 0, 10)
TeleportContainer.BackgroundTransparency = 1
TeleportContainer.Parent = MainFrame
TeleportContainer.ZIndex = 1
TeleportContainer.Visible = false

local MoneyContainer = Instance.new("Frame")
MoneyContainer.Name = "MoneyContainer"
MoneyContainer.Size = UDim2.new(1, -150, 1, -20)
MoneyContainer.Position = UDim2.new(0, 150, 0, 10)
MoneyContainer.BackgroundTransparency = 1
MoneyContainer.Parent = MainFrame
MoneyContainer.ZIndex = 1
MoneyContainer.Visible = false

local FishingContainer = Instance.new("Frame")
FishingContainer.Name = "FishingContainer"
FishingContainer.Size = UDim2.new(1, -150, 1, -20)
FishingContainer.Position = UDim2.new(0, 150, 0, 10)
FishingContainer.BackgroundTransparency = 1
FishingContainer.Parent = MainFrame
FishingContainer.ZIndex = 1
FishingContainer.Visible = false

local PlayerContainer = Instance.new("Frame")
PlayerContainer.Name = "PlayerContainer"
PlayerContainer.Size = UDim2.new(1, -150, 1, -20)
PlayerContainer.Position = UDim2.new(0, 150, 0, 10)
PlayerContainer.BackgroundTransparency = 1
PlayerContainer.Parent = MainFrame
PlayerContainer.ZIndex = 1
PlayerContainer.Visible = false

local SettingsContainer = Instance.new("Frame")
SettingsContainer.Name = "SettingsContainer"
SettingsContainer.Size = UDim2.new(1, -150, 1, -20)
SettingsContainer.Position = UDim2.new(0, 150, 0, 10)
SettingsContainer.BackgroundTransparency = 1
SettingsContainer.Parent = MainFrame
SettingsContainer.ZIndex = 1
SettingsContainer.Visible = false

-- ============================================
-- حاوية المعلومات (جديدة)
-- ============================================

local InfoContainer = Instance.new("Frame")
InfoContainer.Name = "InfoContainer"
InfoContainer.Size = UDim2.new(1, -150, 1, -20)
InfoContainer.Position = UDim2.new(0, 150, 0, 10)
InfoContainer.BackgroundTransparency = 1
InfoContainer.Parent = MainFrame
InfoContainer.ZIndex = 1
InfoContainer.Visible = false

-- ============================================
-- حاوية الأخبار
-- ============================================

local NewsContainer = Instance.new("Frame")
NewsContainer.Name = "NewsContainer"
NewsContainer.Size = UDim2.new(1, -150, 1, -20)
NewsContainer.Position = UDim2.new(0, 150, 0, 10)
NewsContainer.BackgroundTransparency = 1
NewsContainer.Parent = MainFrame
NewsContainer.ZIndex = 1
NewsContainer.Visible = false

-- ============================================
-- محتوى الأخبار
-- ============================================

local NewsFrame = Instance.new("Frame")
NewsFrame.Size = UDim2.new(0.92, 0, 0.88, 0)
NewsFrame.Position = UDim2.new(0.04, 0, 0.04, 0)
NewsFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
NewsFrame.BorderSizePixel = 0
NewsFrame.Parent = NewsContainer
NewsFrame.ZIndex = 5

local NewsFrameCorner = Instance.new("UICorner")
NewsFrameCorner.CornerRadius = UDim.new(0, 12)
NewsFrameCorner.Parent = NewsFrame

local NewsGlow = Instance.new("Frame")
NewsGlow.Size = UDim2.new(1, 6, 1, 6)
NewsGlow.Position = UDim2.new(0, -3, 0, -3)
NewsGlow.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
NewsGlow.BackgroundTransparency = 0.3
NewsGlow.BorderSizePixel = 0
NewsGlow.Parent = NewsFrame
NewsGlow.ZIndex = -1

local NewsGlowCorner = Instance.new("UICorner")
NewsGlowCorner.CornerRadius = UDim.new(0, 15)
NewsGlowCorner.Parent = NewsGlow

local NewsTitle = Instance.new("TextLabel")
NewsTitle.Size = UDim2.new(1, -40, 0, 40)
NewsTitle.Position = UDim2.new(0, 20, 0, 5)
NewsTitle.BackgroundTransparency = 1
NewsTitle.Text = "📰 آخر الأخبار والتحديثات"
NewsTitle.TextColor3 = Color3.fromRGB(255, 200, 0)
NewsTitle.TextSize = 20
NewsTitle.Font = Enum.Font.GothamBold
NewsTitle.TextXAlignment = Enum.TextXAlignment.Center
NewsTitle.Parent = NewsFrame
NewsTitle.ZIndex = 6

local NewsDivider = Instance.new("Frame")
NewsDivider.Size = UDim2.new(0.7, 0, 0, 2)
NewsDivider.Position = UDim2.new(0.15, 0, 0, 50)
NewsDivider.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
NewsDivider.BackgroundTransparency = 0.4
NewsDivider.BorderSizePixel = 0
NewsDivider.Parent = NewsFrame
NewsDivider.ZIndex = 6

local NewsScroll = Instance.new("ScrollingFrame")
NewsScroll.Size = UDim2.new(1, -10, 1, -75)
NewsScroll.Position = UDim2.new(0, 5, 0, 55)
NewsScroll.BackgroundTransparency = 1
NewsScroll.BorderSizePixel = 0
NewsScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
NewsScroll.ScrollBarThickness = 4
NewsScroll.Parent = NewsFrame
NewsScroll.ZIndex = 6

local newsY = 5

local function createNewsCard(icon, title, desc, color, isImportant)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0.94, 0, 0, isImportant and 70 or 60)
    card.Position = UDim2.new(0.03, 0, 0, newsY)
    card.BackgroundColor3 = isImportant and Color3.fromRGB(40, 20, 20) or Color3.fromRGB(28, 28, 42)
    card.BorderSizePixel = 0
    card.Parent = NewsScroll
    card.ZIndex = 7
    
    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card
    
    if isImportant then
        local glowBar = Instance.new("Frame")
        glowBar.Size = UDim2.new(0, 4, 1, -8)
        glowBar.Position = UDim2.new(0, 0, 0, 4)
        glowBar.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
        glowBar.BorderSizePixel = 0
        glowBar.Parent = card
        glowBar.ZIndex = 8
        
        local glowBarCorner = Instance.new("UICorner")
        glowBarCorner.CornerRadius = UDim.new(0, 4)
        glowBarCorner.Parent = glowBar
    end
    
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Size = UDim2.new(0, 40, 1, 0)
    iconLabel.Position = UDim2.new(0, 6, 0, 0)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Text = icon
    iconLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
    iconLabel.TextSize = isImportant and 26 or 22
    iconLabel.Font = Enum.Font.GothamBold
    iconLabel.TextXAlignment = Enum.TextXAlignment.Center
    iconLabel.Parent = card
    iconLabel.ZIndex = 8
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -55, 0, 22)
    titleLabel.Position = UDim2.new(0, 50, 0, 4)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = title
    titleLabel.TextColor3 = isImportant and Color3.fromRGB(255, 100, 100) or Color3.fromRGB(255, 200, 0)
    titleLabel.TextSize = isImportant and 15 or 14
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = card
    titleLabel.ZIndex = 8
    
    local descLabel = Instance.new("TextLabel")
    descLabel.Size = UDim2.new(1, -55, 0, isImportant and 34 or 28)
    descLabel.Position = UDim2.new(0, 50, 0, 26)
    descLabel.BackgroundTransparency = 1
    descLabel.Text = desc
    descLabel.TextColor3 = isImportant and Color3.fromRGB(255, 200, 200) or Color3.fromRGB(180, 180, 200)
    descLabel.TextSize = 12
    descLabel.Font = Enum.Font.GothamMedium
    descLabel.TextXAlignment = Enum.TextXAlignment.Left
    descLabel.TextWrapped = true
    descLabel.Parent = card
    descLabel.ZIndex = 8
    
    newsY = newsY + (isImportant and 78 or 68)
    return card
end

-- إضافة الأخبار
createNewsCard("✅", "تم إضافة نظام سرقة الفلوس التلقائي V7!",
    "الآن يمكنك تجميع الفلوس من الأرض تلقائياً وبسرعة فائقة! يدعم جميع أنواع الفلوس ويتجاهل الطاولات والمقاعد.",
    Color3.fromRGB(0, 255, 100), true)

createNewsCard("🎣", "تم إضافة نظام الصيد التلقائي!",
    "اضغط على زر 'تشغيل الصيد' وابدأ بصيد الأسماك تلقائياً دون عناء! يعمل مع جميع أنواع الصنارات.",
    Color3.fromRGB(0, 200, 255), false)

createNewsCard("🔄", "تحسين أداء السكربت بنسبة 40%!",
    "قمنا بتحسين الكود وتقليل استهلاك المعالج ليعمل بسلاسة حتى على الأجهزة الضعيفة.",
    Color3.fromRGB(100, 200, 255), false)

createNewsCard("📱", "دعم كامل للهاتف والكمبيوتر!",
    "السكربت الآن يدعم جميع الأجهزة مع واجهة متجاوبة وسهلة الاستخدام.",
    Color3.fromRGB(100, 255, 200), false)

createNewsCard("⏳", "🔜 قريباً: نظام الكلبشة التلقائي!",
    "جاري العمل على نظام كلبشة تلقائي للاعبين سيتم إضافته في التحديث القادم.",
    Color3.fromRGB(255, 200, 0), false)

createNewsCard("⏳", "🔜 قريباً: نظام الحماية من الحظر!",
    "سنضيف نظام حماية متقدم يمنع اكتشاف السكربت في السيرفرات.",
    Color3.fromRGB(255, 200, 0), false)

createNewsCard("⏳", "🔜 قريباً: نظام التجميع التلقائي للموارد!",
    "سيتم إضافة نظام يجمع جميع الموارد حولك تلقائياً.",
    Color3.fromRGB(255, 200, 0), false)

createNewsCard("⏳", "🔜 قريباً: نظام التليفون التلقائي!",
    "جاري العمل على نظام يرد على المكالمات تلقائياً.",
    Color3.fromRGB(255, 200, 0), false)

createNewsCard("⏳", "🔜 قريباً: نظام القيادة التلقائي!",
    "سيتم إضافة نظام يقود السيارات تلقائياً.",
    Color3.fromRGB(255, 200, 0), false)

-- قسم الإبلاغ عن الأخطاء
local ReportSection = Instance.new("Frame")
ReportSection.Size = UDim2.new(0.94, 0, 0, 90)
ReportSection.Position = UDim2.new(0.03, 0, 0, newsY + 5)
ReportSection.BackgroundColor3 = Color3.fromRGB(30, 20, 30)
ReportSection.BorderSizePixel = 0
ReportSection.Parent = NewsScroll
ReportSection.ZIndex = 7

local ReportCorner = Instance.new("UICorner")
ReportCorner.CornerRadius = UDim.new(0, 8)
ReportCorner.Parent = ReportSection

local ReportIcon = Instance.new("TextLabel")
ReportIcon.Size = UDim2.new(0, 40, 1, 0)
ReportIcon.Position = UDim2.new(0, 6, 0, 0)
ReportIcon.BackgroundTransparency = 1
ReportIcon.Text = "🐛"
ReportIcon.TextColor3 = Color3.fromRGB(255, 200, 100)
ReportIcon.TextSize = 28
ReportIcon.Font = Enum.Font.GothamBold
ReportIcon.TextXAlignment = Enum.TextXAlignment.Center
ReportIcon.Parent = ReportSection
ReportIcon.ZIndex = 8

local ReportTitle = Instance.new("TextLabel")
ReportTitle.Size = UDim2.new(1, -55, 0, 22)
ReportTitle.Position = UDim2.new(0, 50, 0, 4)
ReportTitle.BackgroundTransparency = 1
ReportTitle.Text = "🐛 الإبلاغ عن أخطاء"
ReportTitle.TextColor3 = Color3.fromRGB(255, 200, 100)
ReportTitle.TextSize = 15
ReportTitle.Font = Enum.Font.GothamBold
ReportTitle.TextXAlignment = Enum.TextXAlignment.Left
ReportTitle.Parent = ReportSection
ReportTitle.ZIndex = 8

local ReportDesc = Instance.new("TextLabel")
ReportDesc.Size = UDim2.new(1, -55, 0, 28)
ReportDesc.Position = UDim2.new(0, 50, 0, 26)
ReportDesc.BackgroundTransparency = 1
ReportDesc.Text = "إذا وجدت أي خطأ في السكربت، أرسل تفاصيله في سيرفر الديسكورد."
ReportDesc.TextColor3 = Color3.fromRGB(180, 180, 200)
ReportDesc.TextSize = 11
ReportDesc.Font = Enum.Font.GothamMedium
ReportDesc.TextXAlignment = Enum.TextXAlignment.Left
ReportDesc.TextWrapped = true
ReportDesc.Parent = ReportSection
ReportDesc.ZIndex = 8

newsY = newsY + 100

-- قسم الديسكورد
local DiscordSection = Instance.new("Frame")
DiscordSection.Size = UDim2.new(0.94, 0, 0, 75)
DiscordSection.Position = UDim2.new(0.03, 0, 0, newsY + 5)
DiscordSection.BackgroundColor3 = Color3.fromRGB(20, 30, 50)
DiscordSection.BorderSizePixel = 0
DiscordSection.Parent = NewsScroll
DiscordSection.ZIndex = 7

local DiscordCorner = Instance.new("UICorner")
DiscordCorner.CornerRadius = UDim.new(0, 8)
DiscordCorner.Parent = DiscordSection

local DiscordGlow2 = Instance.new("Frame")
DiscordGlow2.Size = UDim2.new(1, 6, 1, 6)
DiscordGlow2.Position = UDim2.new(0, -3, 0, -3)
DiscordGlow2.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordGlow2.BackgroundTransparency = 0.3
DiscordGlow2.BorderSizePixel = 0
DiscordGlow2.Parent = DiscordSection
DiscordGlow2.ZIndex = -1

local DiscordGlowCorner2 = Instance.new("UICorner")
DiscordGlowCorner2.CornerRadius = UDim.new(0, 10)
DiscordGlowCorner2.Parent = DiscordGlow2

local DiscordIcon = Instance.new("TextLabel")
DiscordIcon.Size = UDim2.new(0, 45, 1, 0)
DiscordIcon.Position = UDim2.new(0, 6, 0, 0)
DiscordIcon.BackgroundTransparency = 1
DiscordIcon.Text = "💬"
DiscordIcon.TextColor3 = Color3.fromRGB(88, 101, 242)
DiscordIcon.TextSize = 30
DiscordIcon.Font = Enum.Font.GothamBold
DiscordIcon.TextXAlignment = Enum.TextXAlignment.Center
DiscordIcon.Parent = DiscordSection
DiscordIcon.ZIndex = 8

local DiscordTitle = Instance.new("TextLabel")
DiscordTitle.Size = UDim2.new(1, -60, 0, 22)
DiscordTitle.Position = UDim2.new(0, 55, 0, 4)
DiscordTitle.BackgroundTransparency = 1
DiscordTitle.Text = "💬 سيرفر ديسكورد"
DiscordTitle.TextColor3 = Color3.fromRGB(88, 101, 242)
DiscordTitle.TextSize = 15
DiscordTitle.Font = Enum.Font.GothamBold
DiscordTitle.TextXAlignment = Enum.TextXAlignment.Left
DiscordTitle.Parent = DiscordSection
DiscordTitle.ZIndex = 8

local DiscordLink = Instance.new("TextButton")
DiscordLink.Size = UDim2.new(1, -60, 0, 30)
DiscordLink.Position = UDim2.new(0, 55, 0, 30)
DiscordLink.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordLink.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordLink.TextSize = 12
DiscordLink.Font = Enum.Font.GothamBold
DiscordLink.Text = "🔗 https://discord.gg/w9FDeSx9f"
DiscordLink.Parent = DiscordSection
DiscordLink.ZIndex = 8

local DiscordLinkCorner = Instance.new("UICorner")
DiscordLinkCorner.CornerRadius = UDim.new(0, 6)
DiscordLinkCorner.Parent = DiscordLink

DiscordLink.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/w9FDeSx9f")
    end
    showNotification("✅ تم نسخ رابط الديسكورد!", NewsFrame)
end)

DiscordLink.MouseEnter:Connect(function()
    DiscordLink.BackgroundColor3 = Color3.fromRGB(110, 120, 260)
end)
DiscordLink.MouseLeave:Connect(function()
    DiscordLink.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
end)

newsY = newsY + 85

-- قسم الأخطاء الشائعة
local ErrorSection = Instance.new("Frame")
ErrorSection.Size = UDim2.new(0.94, 0, 0, 65)
ErrorSection.Position = UDim2.new(0.03, 0, 0, newsY + 5)
ErrorSection.BackgroundColor3 = Color3.fromRGB(40, 20, 20)
ErrorSection.BorderSizePixel = 0
ErrorSection.Parent = NewsScroll
ErrorSection.ZIndex = 7

local ErrorCorner = Instance.new("UICorner")
ErrorCorner.CornerRadius = UDim.new(0, 8)
ErrorCorner.Parent = ErrorSection

local ErrorIcon2 = Instance.new("TextLabel")
ErrorIcon2.Size = UDim2.new(0, 40, 1, 0)
ErrorIcon2.Position = UDim2.new(0, 6, 0, 0)
ErrorIcon2.BackgroundTransparency = 1
ErrorIcon2.Text = "⚠️"
ErrorIcon2.TextColor3 = Color3.fromRGB(255, 200, 50)
ErrorIcon2.TextSize = 26
ErrorIcon2.Font = Enum.Font.GothamBold
ErrorIcon2.TextXAlignment = Enum.TextXAlignment.Center
ErrorIcon2.Parent = ErrorSection
ErrorIcon2.ZIndex = 8

local ErrorTitle2 = Instance.new("TextLabel")
ErrorTitle2.Size = UDim2.new(1, -55, 0, 22)
ErrorTitle2.Position = UDim2.new(0, 50, 0, 4)
ErrorTitle2.BackgroundTransparency = 1
ErrorTitle2.Text = "⚠️ أخطاء معروفة وحلولها"
ErrorTitle2.TextColor3 = Color3.fromRGB(255, 200, 50)
ErrorTitle2.TextSize = 14
ErrorTitle2.Font = Enum.Font.GothamBold
ErrorTitle2.TextXAlignment = Enum.TextXAlignment.Left
ErrorTitle2.Parent = ErrorSection
ErrorTitle2.ZIndex = 8

local ErrorDesc2 = Instance.new("TextLabel")
ErrorDesc2.Size = UDim2.new(1, -55, 0, 32)
ErrorDesc2.Position = UDim2.new(0, 50, 0, 26)
ErrorDesc2.BackgroundTransparency = 1
ErrorDesc2.Text = "• مشكلة اختفاء الزر: أعد تشغيل السكربت • مشكلة جمع الفلوس: تأكد من وجود فلوس حولك"
ErrorDesc2.TextColor3 = Color3.fromRGB(200, 180, 180)
ErrorDesc2.TextSize = 11
ErrorDesc2.Font = Enum.Font.GothamMedium
ErrorDesc2.TextXAlignment = Enum.TextXAlignment.Left
ErrorDesc2.TextWrapped = true
ErrorDesc2.Parent = ErrorSection
ErrorDesc2.ZIndex = 8

newsY = newsY + 75
NewsScroll.CanvasSize = UDim2.new(0, 0, 0, newsY + 30)

-- دالة عرض الأخبار
local function ShowNews()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = true
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "📰 الأخبار" then
                child.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
                child.TextColor3 = Color3.fromRGB(0, 0, 0)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

-- ============================================
-- محتوى صفحة المعلومات (مع المطورين وصورة الديسكورد)
-- ============================================

local InfoFrame = Instance.new("Frame")
InfoFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
InfoFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
InfoFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
InfoFrame.BorderSizePixel = 0
InfoFrame.Parent = InfoContainer
InfoFrame.ZIndex = 5

local InfoFrameCorner = Instance.new("UICorner")
InfoFrameCorner.CornerRadius = UDim.new(0, 12)
InfoFrameCorner.Parent = InfoFrame

-- إطار متوهج
local InfoGlow = Instance.new("Frame")
InfoGlow.Size = UDim2.new(1, 6, 1, 6)
InfoGlow.Position = UDim2.new(0, -3, 0, -3)
InfoGlow.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
InfoGlow.BackgroundTransparency = 0.3
InfoGlow.BorderSizePixel = 0
InfoGlow.Parent = InfoFrame
InfoGlow.ZIndex = -1

local InfoGlowCorner = Instance.new("UICorner")
InfoGlowCorner.CornerRadius = UDim.new(0, 15)
InfoGlowCorner.Parent = InfoGlow

-- عنوان الصفحة
local InfoTitle = Instance.new("TextLabel")
InfoTitle.Size = UDim2.new(1, -40, 0, 40)
InfoTitle.Position = UDim2.new(0, 20, 0, 8)
InfoTitle.BackgroundTransparency = 1
InfoTitle.Text = "📋 معلومات السكربت"
InfoTitle.TextColor3 = Color3.fromRGB(0, 200, 255)
InfoTitle.TextSize = 22
InfoTitle.Font = Enum.Font.GothamBold
InfoTitle.TextXAlignment = Enum.TextXAlignment.Center
InfoTitle.Parent = InfoFrame
InfoTitle.ZIndex = 6

-- خط فاصل
local InfoDivider = Instance.new("Frame")
InfoDivider.Size = UDim2.new(0.6, 0, 0, 2)
InfoDivider.Position = UDim2.new(0.2, 0, 0, 52)
InfoDivider.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
InfoDivider.BackgroundTransparency = 0.4
InfoDivider.BorderSizePixel = 0
InfoDivider.Parent = InfoFrame
InfoDivider.ZIndex = 6

-- حاوية البطاقات
local InfoScroll = Instance.new("ScrollingFrame")
InfoScroll.Size = UDim2.new(1, -10, 1, -75)
InfoScroll.Position = UDim2.new(0, 5, 0, 58)
InfoScroll.BackgroundTransparency = 1
InfoScroll.BorderSizePixel = 0
InfoScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
InfoScroll.ScrollBarThickness = 4
InfoScroll.Parent = InfoFrame
InfoScroll.ZIndex = 6

-- دالة إنشاء بطاقة معلومات
local function createInfoCard2(icon, title, value, color)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0.92, 0, 0, 55)
    card.Position = UDim2.new(0.04, 0, 0, 5)
    card.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    card.BorderSizePixel = 0
    card.Parent = InfoScroll
    card.ZIndex = 7
    
    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card
    
    -- أيقونة
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Size = UDim2.new(0, 45, 1, 0)
    iconLabel.Position = UDim2.new(0, 8, 0, 0)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Text = icon
    iconLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
    iconLabel.TextSize = 28
    iconLabel.Font = Enum.Font.GothamBold
    iconLabel.TextXAlignment = Enum.TextXAlignment.Center
    iconLabel.Parent = card
    iconLabel.ZIndex = 8
    
    -- العنوان
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -60, 0, 20)
    titleLabel.Position = UDim2.new(0, 55, 0, 4)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = title
    titleLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
    titleLabel.TextSize = 12
    titleLabel.Font = Enum.Font.GothamMedium
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = card
    titleLabel.ZIndex = 8
    
    -- القيمة
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(1, -60, 0, 24)
    valueLabel.Position = UDim2.new(0, 55, 0, 24)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = value
    valueLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    valueLabel.TextSize = 15
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextXAlignment = Enum.TextXAlignment.Left
    valueLabel.TextWrapped = true
    valueLabel.Parent = card
    valueLabel.ZIndex = 8
    
    return card
end

-- دالة إنشاء بطاقة مطور مع صورة الديسكورد
local function createDeveloperCard(icon, title, discordName, role, color, isOwner)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0.92, 0, 0, 70)
    card.Position = UDim2.new(0.04, 0, 0, 5)
    card.BackgroundColor3 = isOwner and Color3.fromRGB(40, 20, 20) or Color3.fromRGB(20, 30, 50)
    card.BorderSizePixel = 0
    card.Parent = InfoScroll
    card.ZIndex = 7
    
    -- حدود ذهبية للأونر
    if isOwner then
        local border = Instance.new("Frame")
        border.Size = UDim2.new(1, 0, 1, 0)
        border.Position = UDim2.new(0, 0, 0, 0)
        border.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
        border.BackgroundTransparency = 0.6
        border.BorderSizePixel = 0
        border.Parent = card
        border.ZIndex = 6
        
        local borderCorner = Instance.new("UICorner")
        borderCorner.CornerRadius = UDim.new(0, 8)
        borderCorner.Parent = border
    end
    
    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card
    
    -- أيقونة
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Size = UDim2.new(0, 45, 1, 0)
    iconLabel.Position = UDim2.new(0, 8, 0, 0)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Text = icon
    iconLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
    iconLabel.TextSize = 30
    iconLabel.Font = Enum.Font.GothamBold
    iconLabel.TextXAlignment = Enum.TextXAlignment.Center
    iconLabel.Parent = card
    iconLabel.ZIndex = 8
    
    -- دور المطور
    local roleLabel = Instance.new("TextLabel")
    roleLabel.Size = UDim2.new(1, -60, 0, 18)
    roleLabel.Position = UDim2.new(0, 55, 0, 4)
    roleLabel.BackgroundTransparency = 1
    roleLabel.Text = role
    roleLabel.TextColor3 = isOwner and Color3.fromRGB(255, 215, 0) or Color3.fromRGB(88, 101, 242)
    roleLabel.TextSize = 11
    roleLabel.Font = Enum.Font.GothamBold
    roleLabel.TextXAlignment = Enum.TextXAlignment.Left
    roleLabel.Parent = card
    roleLabel.ZIndex = 8
    
    -- اسم الديسكورد
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, -60, 0, 24)
    nameLabel.Position = UDim2.new(0, 55, 0, 24)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = discordName
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextSize = 16
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.TextXAlignment = Enum.TextXAlignment.Left
    nameLabel.Parent = card
    nameLabel.ZIndex = 8
    
    -- زر نسخ الديسكورد
    local copyBtn = Instance.new("TextButton")
    copyBtn.Size = UDim2.new(0, 30, 0, 30)
    copyBtn.Position = UDim2.new(1, -38, 0.5, -15)
    copyBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.TextSize = 16
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.Text = "📋"
    copyBtn.Parent = card
    copyBtn.ZIndex = 9
    
    local copyCorner = Instance.new("UICorner")
    copyCorner.CornerRadius = UDim.new(0, 6)
    copyCorner.Parent = copyBtn
    
    copyBtn.MouseEnter:Connect(function()
        copyBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 95)
    end)
    copyBtn.MouseLeave:Connect(function()
        copyBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    end)
    
    copyBtn.MouseButton1Click:Connect(function()
        if setclipboard then
            setclipboard(discordName)
            ShowInfoNotification("✅ تم نسخ ديسكورد: " .. discordName)
        end
    end)
    
    return card
end

-- إضافة البطاقات
local infoY = 5

-- السكربت
local card1 = createInfoCard2("📦", "إصدار السكربت", "7MO HUB v15 - النسخة النهائية", Color3.fromRGB(180, 0, 255))
card1.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 62

-- مطور السكربت (hamza.abc0)
local cardDev = createDeveloperCard(
    "👨‍💻", 
    "مطور السكربت", 
    "hamza.abc0", 
    "👑 المطور الرئيسي", 
    Color3.fromRGB(180, 0, 255),
    false
)
cardDev.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 77

-- أونر الديسكورد (qóyt.)
local cardOwner = createDeveloperCard(
    "👑", 
    "أونر الديسكورد", 
    "qóyt.", 
    "⭐ مالك السيرفر", 
    Color3.fromRGB(255, 215, 0),
    true
)
cardOwner.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 77

-- صورة الديسكورد (لوجو السيرفر)
local DiscordLogoFrame = Instance.new("Frame")
DiscordLogoFrame.Size = UDim2.new(0.92, 0, 0, 80)
DiscordLogoFrame.Position = UDim2.new(0.04, 0, 0, infoY)
DiscordLogoFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
DiscordLogoFrame.BorderSizePixel = 0
DiscordLogoFrame.Parent = InfoScroll
DiscordLogoFrame.ZIndex = 7

local DiscordLogoCorner = Instance.new("UICorner")
DiscordLogoCorner.CornerRadius = UDim.new(0, 8)
DiscordLogoCorner.Parent = DiscordLogoFrame

-- إطار متوهج للديسكورد
local DiscordGlow3 = Instance.new("Frame")
DiscordGlow3.Size = UDim2.new(1, 6, 1, 6)
DiscordGlow3.Position = UDim2.new(0, -3, 0, -3)
DiscordGlow3.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordGlow3.BackgroundTransparency = 0.3
DiscordGlow3.BorderSizePixel = 0
DiscordGlow3.Parent = DiscordLogoFrame
DiscordGlow3.ZIndex = -1

local DiscordGlowCorner3 = Instance.new("UICorner")
DiscordGlowCorner3.CornerRadius = UDim.new(0, 10)
DiscordGlowCorner3.Parent = DiscordGlow3

-- أيقونة الديسكورد
local DiscordIcon2 = Instance.new("TextLabel")
DiscordIcon2.Size = UDim2.new(0, 60, 1, 0)
DiscordIcon2.Position = UDim2.new(0, 10, 0, 0)
DiscordIcon2.BackgroundTransparency = 1
DiscordIcon2.Text = "💬"
DiscordIcon2.TextColor3 = Color3.fromRGB(88, 101, 242)
DiscordIcon2.TextSize = 40
DiscordIcon2.Font = Enum.Font.GothamBold
DiscordIcon2.TextXAlignment = Enum.TextXAlignment.Center
DiscordIcon2.Parent = DiscordLogoFrame
DiscordIcon2.ZIndex = 8

-- نص الديسكورد
local DiscordText = Instance.new("TextLabel")
DiscordText.Size = UDim2.new(1, -80, 0, 25)
DiscordText.Position = UDim2.new(0, 75, 0, 12)
DiscordText.BackgroundTransparency = 1
DiscordText.Text = "سيرفر الديسكورد الرسمي"
DiscordText.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordText.TextSize = 16
DiscordText.Font = Enum.Font.GothamBold
DiscordText.TextXAlignment = Enum.TextXAlignment.Left
DiscordText.Parent = DiscordLogoFrame
DiscordText.ZIndex = 8

-- رابط الديسكورد
local DiscordLinkText = Instance.new("TextLabel")
DiscordLinkText.Size = UDim2.new(1, -80, 0, 20)
DiscordLinkText.Position = UDim2.new(0, 75, 0, 40)
DiscordLinkText.BackgroundTransparency = 1
DiscordLinkText.Text = "https://discord.gg/w9FDeSx9f"
DiscordLinkText.TextColor3 = Color3.fromRGB(88, 101, 242)
DiscordLinkText.TextSize = 13
DiscordLinkText.Font = Enum.Font.GothamMedium
DiscordLinkText.TextXAlignment = Enum.TextXAlignment.Left
DiscordLinkText.Parent = DiscordLogoFrame
DiscordLinkText.ZIndex = 8

-- زر نسخ الرابط
local CopyLinkBtn = Instance.new("TextButton")
CopyLinkBtn.Size = UDim2.new(0, 50, 0, 30)
CopyLinkBtn.Position = UDim2.new(1, -60, 0.5, -15)
CopyLinkBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
CopyLinkBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CopyLinkBtn.TextSize = 14
CopyLinkBtn.Font = Enum.Font.GothamBold
CopyLinkBtn.Text = "📋"
CopyLinkBtn.Parent = DiscordLogoFrame
CopyLinkBtn.ZIndex = 9

local CopyLinkCorner = Instance.new("UICorner")
CopyLinkCorner.CornerRadius = UDim.new(0, 6)
CopyLinkCorner.Parent = CopyLinkBtn

CopyLinkBtn.MouseEnter:Connect(function()
    CopyLinkBtn.BackgroundColor3 = Color3.fromRGB(110, 120, 260)
end)
CopyLinkBtn.MouseLeave:Connect(function()
    CopyLinkBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
end)

CopyLinkBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/w9FDeSx9f")
        ShowInfoNotification("✅ تم نسخ رابط الديسكورد!")
    end
end)

infoY = infoY + 87

-- عدد الميزات
local card4 = createInfoCard2("⚡", "عدد الميزات", "8 ميزات (سرعة - قفز - جاذبية - اختفاء - اختراق - طيران - دوران - FOV)", Color3.fromRGB(255, 200, 0))
card4.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 62

-- عدد أماكن الانتقال
local card5 = createInfoCard2("📍", "أماكن الانتقال الفوري", tostring(#places) .. " مكان", Color3.fromRGB(0, 255, 150))
card5.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 62

-- حالة السكربت
local card6 = createInfoCard2("✅", "حالة السكربت", "يعمل بكفاءة 100% 🟢", Color3.fromRGB(0, 255, 100))
card6.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 62

-- تاريخ الإصدار
local card7 = createInfoCard2("📅", "آخر تحديث", "18 يوليو 2026", Color3.fromRGB(255, 150, 50))
card7.Position = UDim2.new(0.04, 0, 0, infoY)
infoY = infoY + 62

InfoScroll.CanvasSize = UDim2.new(0, 0, 0, infoY + 30)

-- دالة إشعارات صفحة المعلومات
function ShowInfoNotification(text)
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 280, 0, 35)
    notif.Position = UDim2.new(0.5, -140, 0.85, 0)
    notif.BackgroundColor3 = Color3.fromRGB(0, 80, 0)
    notif.BackgroundTransparency = 0.2
    notif.TextColor3 = Color3.fromRGB(0, 255, 150)
    notif.TextSize = 13
    notif.Font = Enum.Font.GothamBold
    notif.Text = text
    notif.Parent = InfoFrame
    notif.ZIndex = 50
    
    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 8)
    notifCorner.Parent = notif
    
    task.wait(2)
    notif:Destroy()
end

-- ============================================
-- المحتوى حق الصفحة الرئيسية (Home)
-- ============================================

local avatarFrame = Instance.new("Frame")
avatarFrame.Size = UDim2.new(0, 80, 0, 80)
avatarFrame.Position = UDim2.new(0.5, -40, 0, 10)
avatarFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
avatarFrame.BorderSizePixel = 0
avatarFrame.Parent = HomeContainer
avatarFrame.ZIndex = 2

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(0, 40)
AvatarCorner.Parent = avatarFrame

local AvatarGlow = Instance.new("Frame")
AvatarGlow.Size = UDim2.new(1, 8, 1, 8)
AvatarGlow.Position = UDim2.new(0, -4, 0, -4)
AvatarGlow.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
AvatarGlow.BackgroundTransparency = 0.4
AvatarGlow.BorderSizePixel = 0
AvatarGlow.Parent = avatarFrame
AvatarGlow.ZIndex = 1

local AvatarGlowCorner = Instance.new("UICorner")
AvatarGlowCorner.CornerRadius = UDim.new(0, 44)
AvatarGlowCorner.Parent = AvatarGlow

local AvatarImage = Instance.new("ImageLabel")
AvatarImage.Size = UDim2.new(1, -4, 1, -4)
AvatarImage.Position = UDim2.new(0, 2, 0, 2)
AvatarImage.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
AvatarImage.BackgroundTransparency = 0
AvatarImage.Image = "rbxassetid://0"
AvatarImage.Parent = avatarFrame
AvatarImage.ZIndex = 3

local ImageCorner = Instance.new("UICorner")
ImageCorner.CornerRadius = UDim.new(0, 38)
ImageCorner.Parent = AvatarImage

task.spawn(function()
    local success, avatarImg = pcall(function()
        return Players:GetUserThumbnailAsync(UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
    end)
    if success and avatarImg then
        AvatarImage.Image = avatarImg
        AvatarImage.BackgroundTransparency = 1
    end
end)

local DisplayNameLabel = Instance.new("TextLabel")
DisplayNameLabel.Size = UDim2.new(1, -20, 0, 25)
DisplayNameLabel.Position = UDim2.new(0, 10, 0, 98)
DisplayNameLabel.BackgroundTransparency = 1
DisplayNameLabel.Text = LocalPlayer.DisplayName
DisplayNameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
DisplayNameLabel.TextSize = 18
DisplayNameLabel.Font = Enum.Font.GothamBold
DisplayNameLabel.TextXAlignment = Enum.TextXAlignment.Center
DisplayNameLabel.Parent = HomeContainer
DisplayNameLabel.ZIndex = 2

local UsernameLabel = Instance.new("TextLabel")
UsernameLabel.Size = UDim2.new(1, -20, 0, 16)
UsernameLabel.Position = UDim2.new(0, 10, 0, 123)
UsernameLabel.BackgroundTransparency = 1
UsernameLabel.Text = "@" .. LocalPlayer.Name
UsernameLabel.TextColor3 = Color3.fromRGB(180, 0, 255)
UsernameLabel.TextSize = 13
UsernameLabel.Font = Enum.Font.GothamMedium
UsernameLabel.TextXAlignment = Enum.TextXAlignment.Center
UsernameLabel.Parent = HomeContainer
UsernameLabel.ZIndex = 2

local Divider = Instance.new("Frame")
Divider.Size = UDim2.new(0.85, 0, 0, 1)
Divider.Position = UDim2.new(0.075, 0, 0, 100)
Divider.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
Divider.BackgroundTransparency = 0.4
Divider.BorderSizePixel = 0
Divider.Parent = HomeContainer
Divider.ZIndex = 2

local function createInfoCard(title, value, icon, row, col)
    local Card = Instance.new("Frame")
    Card.Size = UDim2.new(0.46, -10, 0, 50)
    Card.Position = UDim2.new(
        col == 1 and 0.02 or 0.52,
        0,
        0,
        148 + ((row - 1) * 60)
    )
    Card.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    Card.BorderSizePixel = 0
    Card.Parent = HomeContainer
    Card.ZIndex = 2

    local CardCorner = Instance.new("UICorner")
    CardCorner.CornerRadius = UDim.new(0, 8)
    CardCorner.Parent = Card

    Card.MouseEnter:Connect(function()
        Card.BackgroundColor3 = Color3.fromRGB(35, 35, 50)
    end)
    Card.MouseLeave:Connect(function()
        Card.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    end)

    local IconLabel = Instance.new("TextLabel")
    IconLabel.Size = UDim2.new(0, 32, 1, 0)
    IconLabel.Position = UDim2.new(0, 6, 0, 0)
    IconLabel.BackgroundTransparency = 1
    IconLabel.Text = icon
    IconLabel.TextColor3 = Color3.fromRGB(180, 0, 255)
    IconLabel.TextSize = 22
    IconLabel.Font = Enum.Font.GothamBold
    IconLabel.TextXAlignment = Enum.TextXAlignment.Center
    IconLabel.Parent = Card
    IconLabel.ZIndex = 3

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, -45, 0, 18)
    TitleLabel.Position = UDim2.new(0, 42, 0, 4)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = title
    TitleLabel.TextColor3 = Color3.fromRGB(180, 0, 255)
    TitleLabel.TextSize = 11
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Parent = Card
    TitleLabel.ZIndex = 3

    local ValueLabel = Instance.new("TextLabel")
    ValueLabel.Size = UDim2.new(1, -45, 0, 22)
    ValueLabel.Position = UDim2.new(0, 42, 0, 22)
    ValueLabel.BackgroundTransparency = 1
    ValueLabel.Text = value
    ValueLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    ValueLabel.TextSize = 13
    ValueLabel.Font = Enum.Font.GothamSemibold
    ValueLabel.TextXAlignment = Enum.TextXAlignment.Left
    ValueLabel.Parent = Card
    ValueLabel.ZIndex = 3
end

local totalDays = LocalPlayer.AccountAge
local parsedYears = math.floor(totalDays / 365)
local extraDays = totalDays % 365
local parsedMonths = math.floor(extraDays / 30)
local finalDays = extraDays % 30
local ageText = string.format("%d سنة, %d شهر, %d يوم", parsedYears, parsedMonths, finalDays)

createInfoCard("اسم العرض", LocalPlayer.DisplayName, "👤", 1, 1)
createInfoCard("اسم المستخدم", "@" .. LocalPlayer.Name, "🔹", 1, 2)
createInfoCard("رقم الحساب", tostring(UserId), "🆔", 2, 1)
createInfoCard("عمر الحساب", ageText, "📅", 2, 2)

local DevContainer = Instance.new("Frame")
DevContainer.Size = UDim2.new(0.9, 0, 0, 40)
DevContainer.Position = UDim2.new(0.05, 0, 0, 280)
DevContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
DevContainer.BorderSizePixel = 0
DevContainer.Parent = HomeContainer
DevContainer.ZIndex = 5

local DevCorner = Instance.new("UICorner")
DevCorner.CornerRadius = UDim.new(0, 8)
DevCorner.Parent = DevContainer

local DevName = Instance.new("TextButton")
DevName.Size = UDim2.new(0.48, -5, 1, -6)
DevName.Position = UDim2.new(0.01, 0, 0, 3)
DevName.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
DevName.BorderSizePixel = 0
DevName.Text = "👨‍💻 hamza000599"
DevName.TextColor3 = Color3.fromRGB(180, 0, 255)
DevName.TextSize = 12
DevName.Font = Enum.Font.GothamBold
DevName.Parent = DevContainer
DevName.ZIndex = 6

local DevNameCorner = Instance.new("UICorner")
DevNameCorner.CornerRadius = UDim.new(0, 6)
DevNameCorner.Parent = DevName

DevName.MouseButton1Click:Connect(function()
    setclipboard("hamza000599")
    local Notification = Instance.new("TextLabel")
    Notification.Size = UDim2.new(0, 200, 0, 30)
    Notification.Position = UDim2.new(0.5, -100, 0.5, -15)
    Notification.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    Notification.BorderSizePixel = 0
    Notification.Text = "✅ تم نسخ اسم المطور!"
    Notification.TextColor3 = Color3.fromRGB(0, 255, 150)
    Notification.TextSize = 14
    Notification.Font = Enum.Font.GothamBold
    Notification.Parent = MainFrame
    Notification.ZIndex = 50
    local NotifCorner = Instance.new("UICorner")
    NotifCorner.CornerRadius = UDim.new(0, 8)
    NotifCorner.Parent = Notification
    task.wait(2)
    Notification:Destroy()
end)

local DevDiscord = Instance.new("TextButton")
DevDiscord.Size = UDim2.new(0.48, -5, 1, -6)
DevDiscord.Position = UDim2.new(0.51, 0, 0, 3)
DevDiscord.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
DevDiscord.BorderSizePixel = 0
DevDiscord.Text = "💬 hamza.abc0"
DevDiscord.TextColor3 = Color3.fromRGB(180, 0, 255)
DevDiscord.TextSize = 12
DevDiscord.Font = Enum.Font.GothamBold
DevDiscord.Parent = DevContainer
DevDiscord.ZIndex = 6

local DevDiscordCorner = Instance.new("UICorner")
DevDiscordCorner.CornerRadius = UDim.new(0, 6)
DevDiscordCorner.Parent = DevDiscord

DevDiscord.MouseButton1Click:Connect(function()
    setclipboard("hamza.abc0")
    local Notification = Instance.new("TextLabel")
    Notification.Size = UDim2.new(0, 220, 0, 30)
    Notification.Position = UDim2.new(0.5, -110, 0.5, -15)
    Notification.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    Notification.BorderSizePixel = 0
    Notification.Text = "✅ تم نسخ ديسكورد المطور!"
    Notification.TextColor3 = Color3.fromRGB(0, 255, 150)
    Notification.TextSize = 14
    Notification.Font = Enum.Font.GothamBold
    Notification.Parent = MainFrame
    Notification.ZIndex = 50
    local NotifCorner = Instance.new("UICorner")
    NotifCorner.CornerRadius = UDim.new(0, 8)
    NotifCorner.Parent = Notification
    task.wait(2)
    Notification:Destroy()
end)

local FooterText = Instance.new("TextLabel")
FooterText.Size = UDim2.new(1, -20, 0, 16)
FooterText.Position = UDim2.new(0, 10, 1, -18)
FooterText.BackgroundTransparency = 1
FooterText.Text = "✦ 7MO HUB v15 ✦"
FooterText.TextColor3 = Color3.fromRGB(180, 0, 255)
FooterText.TextSize = 10
FooterText.Font = Enum.Font.GothamMedium
FooterText.TextXAlignment = Enum.TextXAlignment.Center
FooterText.TextTransparency = 0.4
FooterText.Parent = HomeContainer
FooterText.ZIndex = 2

-- ============================================
-- محتوى صفحة الترحيب (Welcome)
-- ============================================

local WelcomeFrame = Instance.new("Frame")
WelcomeFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
WelcomeFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
WelcomeFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
WelcomeFrame.BorderSizePixel = 0
WelcomeFrame.Parent = WelcomeContainer
WelcomeFrame.ZIndex = 5

local WelcomeFrameCorner = Instance.new("UICorner")
WelcomeFrameCorner.CornerRadius = UDim.new(0, 12)
WelcomeFrameCorner.Parent = WelcomeFrame

local WelcomeGlow = Instance.new("Frame")
WelcomeGlow.Size = UDim2.new(1, 6, 1, 6)
WelcomeGlow.Position = UDim2.new(0, -3, 0, -3)
WelcomeGlow.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
WelcomeGlow.BackgroundTransparency = 0.3
WelcomeGlow.BorderSizePixel = 0
WelcomeGlow.Parent = WelcomeFrame
WelcomeGlow.ZIndex = -1

local WelcomeGlowCorner = Instance.new("UICorner")
WelcomeGlowCorner.CornerRadius = UDim.new(0, 15)
WelcomeGlowCorner.Parent = WelcomeGlow

local WelcomeIcon = Instance.new("TextLabel")
WelcomeIcon.Size = UDim2.new(0, 80, 0, 80)
WelcomeIcon.Position = UDim2.new(0.5, -40, 0, 30)
WelcomeIcon.BackgroundTransparency = 1
WelcomeIcon.Text = "🎉"
WelcomeIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
WelcomeIcon.TextSize = 70
WelcomeIcon.Font = Enum.Font.GothamBold
WelcomeIcon.TextXAlignment = Enum.TextXAlignment.Center
WelcomeIcon.Parent = WelcomeFrame
WelcomeIcon.ZIndex = 6

local WelcomeTitle = Instance.new("TextLabel")
WelcomeTitle.Size = UDim2.new(1, -40, 0, 40)
WelcomeTitle.Position = UDim2.new(0, 20, 0, 130)
WelcomeTitle.BackgroundTransparency = 1
WelcomeTitle.Text = "🌟 نورت سكربت 7MO HUB v15 🌟"
WelcomeTitle.TextColor3 = Color3.fromRGB(180, 0, 255)
WelcomeTitle.TextSize = 22
WelcomeTitle.Font = Enum.Font.GothamBold
WelcomeTitle.TextXAlignment = Enum.TextXAlignment.Center
WelcomeTitle.Parent = WelcomeFrame
WelcomeTitle.ZIndex = 6

local WelcomeMessage = Instance.new("TextLabel")
WelcomeMessage.Size = UDim2.new(1, -40, 0, 30)
WelcomeMessage.Position = UDim2.new(0, 20, 0, 180)
WelcomeMessage.BackgroundTransparency = 1
WelcomeMessage.Text = "✨ مرحباً بك في أقوى سكربت على الإطلاق ✨"
WelcomeMessage.TextColor3 = Color3.fromRGB(200, 200, 200)
WelcomeMessage.TextSize = 16
WelcomeMessage.Font = Enum.Font.GothamMedium
WelcomeMessage.TextXAlignment = Enum.TextXAlignment.Center
WelcomeMessage.Parent = WelcomeFrame
WelcomeMessage.ZIndex = 6

local WelcomePlayer = Instance.new("TextLabel")
WelcomePlayer.Size = UDim2.new(1, -40, 0, 30)
WelcomePlayer.Position = UDim2.new(0, 20, 0, 220)
WelcomePlayer.BackgroundTransparency = 1
WelcomePlayer.Text = "👤 " .. LocalPlayer.DisplayName
WelcomePlayer.TextColor3 = Color3.fromRGB(255, 255, 255)
WelcomePlayer.TextSize = 18
WelcomePlayer.Font = Enum.Font.GothamBold
WelcomePlayer.TextXAlignment = Enum.TextXAlignment.Center
WelcomePlayer.Parent = WelcomeFrame
WelcomePlayer.ZIndex = 6

-- ============================================
-- محتوى صفحة الانتقال الفوري (Teleport)
-- ============================================

local TeleportFrame = Instance.new("Frame")
TeleportFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
TeleportFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
TeleportFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
TeleportFrame.BorderSizePixel = 0
TeleportFrame.Parent = TeleportContainer
TeleportFrame.ZIndex = 5

local TeleportFrameCorner = Instance.new("UICorner")
TeleportFrameCorner.CornerRadius = UDim.new(0, 12)
TeleportFrameCorner.Parent = TeleportFrame

local TeleportGlow = Instance.new("Frame")
TeleportGlow.Size = UDim2.new(1, 6, 1, 6)
TeleportGlow.Position = UDim2.new(0, -3, 0, -3)
TeleportGlow.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
TeleportGlow.BackgroundTransparency = 0.3
TeleportGlow.BorderSizePixel = 0
TeleportGlow.Parent = TeleportFrame
TeleportGlow.ZIndex = -1

local TeleportGlowCorner = Instance.new("UICorner")
TeleportGlowCorner.CornerRadius = UDim.new(0, 15)
TeleportGlowCorner.Parent = TeleportGlow

local TeleportTitle = Instance.new("TextLabel")
TeleportTitle.Size = UDim2.new(1, -40, 0, 35)
TeleportTitle.Position = UDim2.new(0, 20, 0, 8)
TeleportTitle.BackgroundTransparency = 1
TeleportTitle.Text = "⚡ انتقال فوري ⚡"
TeleportTitle.TextColor3 = Color3.fromRGB(180, 0, 255)
TeleportTitle.TextSize = 20
TeleportTitle.Font = Enum.Font.GothamBold
TeleportTitle.TextXAlignment = Enum.TextXAlignment.Center
TeleportTitle.Parent = TeleportFrame
TeleportTitle.ZIndex = 6

local TeleportDivider = Instance.new("Frame")
TeleportDivider.Size = UDim2.new(0.6, 0, 0, 2)
TeleportDivider.Position = UDim2.new(0.2, 0, 0, 48)
TeleportDivider.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
TeleportDivider.BackgroundTransparency = 0.4
TeleportDivider.BorderSizePixel = 0
TeleportDivider.Parent = TeleportFrame
TeleportDivider.ZIndex = 6

local TeleportScroll = Instance.new("ScrollingFrame")
TeleportScroll.Size = UDim2.new(1, -10, 1, -75)
TeleportScroll.Position = UDim2.new(0, 5, 0, 55)
TeleportScroll.BackgroundTransparency = 1
TeleportScroll.BorderSizePixel = 0
TeleportScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
TeleportScroll.ScrollBarThickness = 4
TeleportScroll.Parent = TeleportFrame
TeleportScroll.ZIndex = 6

local places = {
    {name = "🏠 البداية", cf = CFrame.new(-154.62, 5.17, 4.80), color = Color3.fromRGB(100, 100, 100)},
    {name = "☕ الكوفي", cf = CFrame.new(46.80, 4.86, -282.78), color = Color3.fromRGB(0, 180, 100)},
    {name = "⛺ الخيمة", cf = CFrame.new(-308.12, 4.62, -257.02), color = Color3.fromRGB(212, 143, 56)},
    {name = "🎭 المسرح", cf = CFrame.new(-307.16, 8.82, -176.99), color = Color3.fromRGB(0, 120, 200)},
    {name = "🍽️ المطعم", cf = CFrame.new(-153.43, 8.18, 262.25), color = Color3.fromRGB(180, 40, 40)},
    {name = "🛒 المتجر", cf = CFrame.new(-90.87, 5.55, 282.49), color = Color3.fromRGB(120, 50, 160)},
    {name = "❄️ جزيرة الثلج", cf = CFrame.new(-35.88, 5.34, 505.95), color = Color3.fromRGB(0, 190, 220)},
    {name = "🎣 الصيد 1", cf = CFrame.new(-317.16, 15.29, 382.65), color = Color3.fromRGB(139, 94, 60)},
    {name = "🎣 الصيد 2", cf = CFrame.new(-234.03, 5.07, 535.67), color = Color3.fromRGB(160, 114, 75)},
    {name = "🎣 الصيد 3", cf = CFrame.new(-268.61, 5.07, 693.13), color = Color3.fromRGB(180, 135, 95)},
    {name = "🎣 الصيد 4", cf = CFrame.new(197.76, 18.99, 644.84), color = Color3.fromRGB(200, 150, 110)},
    {name = "🚗 متجر السيارات", cf = CFrame.new(238.83, 5.17, 164.67), color = Color3.fromRGB(90, 100, 110)},
    {name = "🏦 البنك 1", cf = CFrame.new(265.02, 5.17, 179.93), color = Color3.fromRGB(15, 110, 70)},
    {name = "🏦 البنك 2", cf = CFrame.new(-128.40, 5.17, 22.61), color = Color3.fromRGB(20, 130, 85)},
    {name = "🏝️ الجزيرة الخاصة", cf = CFrame.new(644.16, 227.76, -98.30), color = Color3.fromRGB(140, 90, 210)},
    {name = "🎹 البيانو", cf = CFrame.new(113.23, 7.01, -383.69), color = Color3.fromRGB(50, 55, 60)},
    {name = "🏟️ الملعب", cf = CFrame.new(142.41, 4.17, 251.22), color = Color3.fromRGB(34, 139, 34)},
    {name = "🐟 متجر السمك", cf = CFrame.new(-106.07, 5.43, 612.25), color = Color3.fromRGB(0, 75, 140)},
    {name = "🏕️ المخيم", cf = CFrame.new(-112.13, 5.43, 635.56), color = Color3.fromRGB(120, 80, 50)},
    {name = "🛖 الكوخ", cf = CFrame.new(106.04, 1.43, 684.97), color = Color3.fromRGB(160, 120, 90)},
    {name = "🏜️ الصحراء", cf = CFrame.new(-33.01, 5.18, -180.50), color = Color3.fromRGB(210, 180, 80)},
    {name = "🎣 الصيد 5", cf = CFrame.new(-59.19, 15.25, 454.89), color = Color3.fromRGB(115, 145, 95)},
}

local function createTeleportButtons()
    local yOffset = 5
    local buttonHeight = 32
    local spacing = 6
    
    for i, place in ipairs(places) do
        local button = Instance.new("TextButton")
        button.Name = "TeleportBtn_" .. i
        button.Size = UDim2.new(0.92, 0, 0, buttonHeight)
        button.Position = UDim2.new(0.04, 0, 0, yOffset)
        button.BackgroundColor3 = place.color
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        button.TextSize = 13
        button.Font = Enum.Font.GothamSemibold
        button.Text = place.name
        button.Parent = TeleportScroll
        button.ZIndex = 7
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 6)
        corner.Parent = button
        
        local origColor = place.color
        button.MouseEnter:Connect(function()
            button.BackgroundColor3 = Color3.new(
                math.min(origColor.R + 0.15, 1),
                math.min(origColor.G + 0.15, 1),
                math.min(origColor.B + 0.15, 1)
            )
        end)
        button.MouseLeave:Connect(function()
            button.BackgroundColor3 = origColor
        end)
        
        button.MouseButton1Click:Connect(function()
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = place.cf
                task.wait(0.1)
                ApplySettings()
            end
        end)
        
        yOffset = yOffset + buttonHeight + spacing
    end
    
    TeleportScroll.CanvasSize = UDim2.new(0, 0, 0, yOffset + 10)
end

createTeleportButtons()

local TeleportFooter = Instance.new("TextLabel")
TeleportFooter.Size = UDim2.new(1, -40, 0, 18)
TeleportFooter.Position = UDim2.new(0, 20, 1, -22)
TeleportFooter.BackgroundTransparency = 1
TeleportFooter.Text = "💜 " .. #places .. " مكاناً للانتقال الفوري"
TeleportFooter.TextColor3 = Color3.fromRGB(150, 150, 180)
TeleportFooter.TextSize = 11
TeleportFooter.Font = Enum.Font.GothamMedium
TeleportFooter.TextXAlignment = Enum.TextXAlignment.Center
TeleportFooter.Parent = TeleportFrame
TeleportFooter.ZIndex = 6

-- ============================================
-- محتوى صفحة الفلوس (Money) - مع سكربت التجميع الفوري
-- ============================================

local MoneyFrame = Instance.new("Frame")
MoneyFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
MoneyFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
MoneyFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MoneyFrame.BorderSizePixel = 0
MoneyFrame.Parent = MoneyContainer
MoneyFrame.ZIndex = 5

local MoneyFrameCorner = Instance.new("UICorner")
MoneyFrameCorner.CornerRadius = UDim.new(0, 12)
MoneyFrameCorner.Parent = MoneyFrame

-- عنوان الصفحة
local MoneyTitle = Instance.new("TextLabel")
MoneyTitle.Size = UDim2.new(1, 0, 0, 35)
MoneyTitle.Position = UDim2.new(0, 0, 0, 5)
MoneyTitle.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MoneyTitle.Text = "💰 فلوس 💰"
MoneyTitle.TextColor3 = Color3.fromRGB(180, 0, 255)
MoneyTitle.TextSize = 18
MoneyTitle.Font = Enum.Font.GothamBold
MoneyTitle.Parent = MoneyFrame
MoneyTitle.ZIndex = 6

-- ============================================
-- سكربت تجميع الفلوس الفوري V8 (محسن)
-- ============================================

-- زر التشغيل/الإيقاف
local ToggleButtonMoney = Instance.new("TextButton")
ToggleButtonMoney.Size = UDim2.new(0.8, 0, 0, 50)
ToggleButtonMoney.Position = UDim2.new(0.1, 0, 0.2, 0)
ToggleButtonMoney.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleButtonMoney.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButtonMoney.TextSize = 24
ToggleButtonMoney.Font = Enum.Font.SourceSansBold
ToggleButtonMoney.Text = "OFF ❌"
ToggleButtonMoney.Parent = MoneyFrame
ToggleButtonMoney.ZIndex = 7

local ToggleCornerMoney = Instance.new("UICorner")
ToggleCornerMoney.CornerRadius = UDim.new(0, 10)
ToggleCornerMoney.Parent = ToggleButtonMoney

-- إحصائيات الفلوس المجمعة
local StatsFrame = Instance.new("Frame")
StatsFrame.Size = UDim2.new(0.9, 0, 0, 60)
StatsFrame.Position = UDim2.new(0.05, 0, 0.5, 0)
StatsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
StatsFrame.BorderSizePixel = 0
StatsFrame.Parent = MoneyFrame
StatsFrame.ZIndex = 7

local StatsCorner = Instance.new("UICorner")
StatsCorner.CornerRadius = UDim.new(0, 8)
StatsCorner.Parent = StatsFrame

-- عدد الرزم المجمعة
local CountLabel = Instance.new("TextLabel")
CountLabel.Size = UDim2.new(0.5, 0, 1, 0)
CountLabel.Position = UDim2.new(0, 0, 0, 0)
CountLabel.BackgroundTransparency = 1
CountLabel.Text = "📦 0"
CountLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
CountLabel.TextSize = 18
CountLabel.Font = Enum.Font.GothamBold
CountLabel.TextXAlignment = Enum.TextXAlignment.Center
CountLabel.Parent = StatsFrame
CountLabel.ZIndex = 8

-- حالة التشغيل
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.5, 0, 1, 0)
StatusLabel.Position = UDim2.new(0.5, 0, 0, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "⏸️ متوقف"
StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
StatusLabel.TextSize = 18
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextXAlignment = Enum.TextXAlignment.Center
StatusLabel.Parent = StatsFrame
StatusLabel.ZIndex = 8

-- إشعار آخر عملية جمع
local LastCollectLabel = Instance.new("TextLabel")
LastCollectLabel.Size = UDim2.new(1, 0, 0, 20)
LastCollectLabel.Position = UDim2.new(0, 0, 0.8, 0)
LastCollectLabel.BackgroundTransparency = 1
LastCollectLabel.Text = "⏳ في انتظار الرزم..."
LastCollectLabel.TextColor3 = Color3.fromRGB(150, 150, 180)
LastCollectLabel.TextSize = 12
LastCollectLabel.Font = Enum.Font.GothamMedium
LastCollectLabel.TextXAlignment = Enum.TextXAlignment.Center
LastCollectLabel.Parent = MoneyFrame
LastCollectLabel.ZIndex = 8

-- متغيرات التشغيل
_G.CashStealer = false
local eventConnection = nil
local collectedCount = 0
local isProcessing = false

-- دالة عرض الإشعارات داخل الصفحة
local function ShowMoneyNotification(text, isSuccess)
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 250, 0, 35)
    notif.Position = UDim2.new(0.5, -125, 0.88, 0)
    notif.BackgroundColor3 = isSuccess and Color3.fromRGB(0, 80, 0) or Color3.fromRGB(80, 0, 0)
    notif.BackgroundTransparency = 0.2
    notif.TextColor3 = isSuccess and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(255, 150, 150)
    notif.TextSize = 13
    notif.Font = Enum.Font.GothamBold
    notif.Text = text
    notif.Parent = MoneyFrame
    notif.ZIndex = 50
    
    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 8)
    notifCorner.Parent = notif
    
    task.wait(1.5)
    notif:Destroy()
end

-- دالة التحقق السريع والالتقاط الخاطف (نسخة محسنة V8)
local function processItem(item)
    if not _G.CashStealer or isProcessing then return end
    isProcessing = true
    
    if item.Name == "CashDrop" and (item:IsA("Part") or item:IsA("MeshPart")) then
        local fullName = item:GetFullName():lower()
        
        if not fullName:find("table") and 
           not fullName:find("board") and 
           not fullName:find("game") and 
           not fullName:find("ludo") and 
           not fullName:find("جلسة") and 
           not fullName:find("طاولة") and
           not fullName:find("seat") and         
           not fullName:find("chair") and        
           not fullName:find("bench") and        
           not fullName:find("lockedframe") and  
           not fullName:find("barrier") then
            
            task.wait(0.05)
            if item:FindFirstChildOfClass("BillboardGui") then
                local player = game.Players.LocalPlayer
                local character = player.Character or player.CharacterAdded:Wait()
                local rootPart = character:FindFirstChild("HumanoidRootPart")
                
                if rootPart then
                    local originalPosition = rootPart.CFrame
                    rootPart.CFrame = item.CFrame
                    
                    local remote = game:GetService("ReplicatedStorage"):FindFirstChild("CashDrop", true)
                    
                    local maxAttempts = 10
                    local attempts = 0
                    local collected = false
                    
                    while item and item.Parent and _G.CashStealer and attempts < maxAttempts do
                        if remote and remote:IsA("RemoteEvent") then
                            remote:FireServer(item)
                        end
                        
                        local prompt = item:FindFirstChildOfClass("ProximityPrompt")
                        if prompt then fireproximityprompt(prompt) end
                        
                        local clicker = item:FindFirstChildOfClass("ClickDetector")
                        if clicker then fireclickdetector(clicker) end
                        
                        attempts = attempts + 1
                        
                        if not item.Parent then
                            collected = true
                            break
                        end
                        
                        task.wait(0.03)
                    end
                    
                    if collected then
                        collectedCount = collectedCount + 1
                        CountLabel.Text = "📦 " .. collectedCount
                        LastCollectLabel.Text = "✅ تم جمع رزمة! (" .. collectedCount .. ")"
                        LastCollectLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
                        ShowMoneyNotification("✅ تم جمع رزمة فلوس!", true)
                    else
                        LastCollectLabel.Text = "⚠️ الرزمة لم تُجمع (مشكلة في السيرفر)"
                        LastCollectLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
                    end
                    
                    if _G.CashStealer then
                        rootPart.CFrame = originalPosition
                    end
                end
            end
        end
    end
    
    isProcessing = false
end

-- تشغيل نظام الرصد
local function startStealing()
    for _, item in pairs(workspace:GetDescendants()) do
        if item.Name == "CashDrop" then
            task.spawn(function() pcall(processItem, item) end)
        end
    end
    
    eventConnection = workspace.DescendantAdded:Connect(function(item)
        if item.Name == "CashDrop" then
            task.spawn(function() pcall(processItem, item) end)
        end
    end)
end

-- زر التشغيل/الإيقاف
ToggleButtonMoney.MouseButton1Click:Connect(function()
    _G.CashStealer = not _G.CashStealer
    if _G.CashStealer then
        ToggleButtonMoney.Text = "ON ✅"
        ToggleButtonMoney.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        StatusLabel.Text = "🟢 يعمل..."
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
        collectedCount = 0
        CountLabel.Text = "📦 0"
        LastCollectLabel.Text = "⏳ جاري التجميع..."
        startStealing()
        ShowMoneyNotification("✅ تم تشغيل تجميع الفلوس الفوري!", true)
    else
        ToggleButtonMoney.Text = "OFF ❌"
        ToggleButtonMoney.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        StatusLabel.Text = "⏸️ متوقف"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        LastCollectLabel.Text = "⏸️ متوقف"
        if eventConnection then
            eventConnection:Disconnect()
            eventConnection = nil
        end
        ShowMoneyNotification("⛔ تم إيقاف تجميع الفلوس", false)
    end
end)

-- زر إعادة تعيين الإحصائيات
local ResetBtn = Instance.new("TextButton")
ResetBtn.Size = UDim2.new(0.3, 0, 0, 30)
ResetBtn.Position = UDim2.new(0.35, 0, 0.72, 0)
ResetBtn.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
ResetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ResetBtn.TextSize = 13
ResetBtn.Font = Enum.Font.SourceSansBold
ResetBtn.Text = "🔄 إعادة تعيين"
ResetBtn.Parent = MoneyFrame
ResetBtn.ZIndex = 7

local ResetCorner = Instance.new("UICorner")
ResetCorner.CornerRadius = UDim.new(0, 6)
ResetCorner.Parent = ResetBtn

ResetBtn.MouseButton1Click:Connect(function()
    collectedCount = 0
    CountLabel.Text = "📦 0"
    LastCollectLabel.Text = "🔄 تم إعادة التعيين"
    ShowMoneyNotification("🔄 تم إعادة تعيين العداد", true)
end)

-- ============================================
-- محتوى صفحة الصيد (Fishing)
-- ============================================

local FishingFrame = Instance.new("Frame")
FishingFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
FishingFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
FishingFrame.BackgroundColor3 = Color3.fromRGB(15, 25, 35)
FishingFrame.BorderSizePixel = 0
FishingFrame.Parent = FishingContainer
FishingFrame.ZIndex = 5

local FishingFrameCorner = Instance.new("UICorner")
FishingFrameCorner.CornerRadius = UDim.new(0, 12)
FishingFrameCorner.Parent = FishingFrame

-- إطار متوهج للصيد
local FishingGlow = Instance.new("Frame")
FishingGlow.Size = UDim2.new(1, 6, 1, 6)
FishingGlow.Position = UDim2.new(0, -3, 0, -3)
FishingGlow.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
FishingGlow.BackgroundTransparency = 0.3
FishingGlow.BorderSizePixel = 0
FishingGlow.Parent = FishingFrame
FishingGlow.ZIndex = -1

local FishingGlowCorner = Instance.new("UICorner")
FishingGlowCorner.CornerRadius = UDim.new(0, 15)
FishingGlowCorner.Parent = FishingGlow

-- عنوان الصفحة
local FishingTitle = Instance.new("TextLabel")
FishingTitle.Size = UDim2.new(1, 0, 0, 40)
FishingTitle.Position = UDim2.new(0, 0, 0, 5)
FishingTitle.BackgroundColor3 = Color3.fromRGB(0, 40, 60)
FishingTitle.Text = "🎣 الصيد التلقائي 🎣"
FishingTitle.TextColor3 = Color3.fromRGB(0, 200, 255)
FishingTitle.TextSize = 20
FishingTitle.Font = Enum.Font.GothamBold
FishingTitle.Parent = FishingFrame
FishingTitle.ZIndex = 6

-- زر تشغيل الصيد
local FishingToggleButton = Instance.new("TextButton")
FishingToggleButton.Size = UDim2.new(0.7, 0, 0, 55)
FishingToggleButton.Position = UDim2.new(0.15, 0, 0.2, 0)
FishingToggleButton.BackgroundColor3 = Color3.fromRGB(0, 150, 200)
FishingToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FishingToggleButton.TextSize = 22
FishingToggleButton.Font = Enum.Font.SourceSansBold
FishingToggleButton.Text = "🎣 تشغيل الصيد"
FishingToggleButton.Parent = FishingFrame
FishingToggleButton.ZIndex = 7

local FishingToggleCorner = Instance.new("UICorner")
FishingToggleCorner.CornerRadius = UDim.new(0, 10)
FishingToggleCorner.Parent = FishingToggleButton

-- حالة الصيد
local FishingStatus = Instance.new("TextLabel")
FishingStatus.Size = UDim2.new(1, -20, 0, 30)
FishingStatus.Position = UDim2.new(0, 10, 0.45, 0)
FishingStatus.BackgroundTransparency = 1
FishingStatus.Text = "⏸️ الصيد متوقف"
FishingStatus.TextColor3 = Color3.fromRGB(255, 100, 100)
FishingStatus.TextSize = 16
FishingStatus.Font = Enum.Font.GothamBold
FishingStatus.TextXAlignment = Enum.TextXAlignment.Center
FishingStatus.Parent = FishingFrame
FishingStatus.ZIndex = 7

-- مربع معلومات
local InfoBox = Instance.new("Frame")
InfoBox.Size = UDim2.new(0.85, 0, 0, 80)
InfoBox.Position = UDim2.new(0.075, 0, 0.55, 0)
InfoBox.BackgroundColor3 = Color3.fromRGB(0, 30, 50)
InfoBox.BorderSizePixel = 0
InfoBox.Parent = FishingFrame
InfoBox.ZIndex = 7

local InfoBoxCorner = Instance.new("UICorner")
InfoBoxCorner.CornerRadius = UDim.new(0, 8)
InfoBoxCorner.Parent = InfoBox

local InfoText = Instance.new("TextLabel")
InfoText.Size = UDim2.new(1, -10, 1, -10)
InfoText.Position = UDim2.new(0, 5, 0, 5)
InfoText.BackgroundTransparency = 1
InfoText.Text = "🐟 سكربت الصيد التلقائي\n📌 اضغط على الزر لتفعيل الصيد التلقائي"
InfoText.TextColor3 = Color3.fromRGB(150, 200, 255)
InfoText.TextSize = 14
InfoText.Font = Enum.Font.GothamMedium
InfoText.TextXAlignment = Enum.TextXAlignment.Center
InfoText.TextYAlignment = Enum.TextYAlignment.Center
InfoText.Parent = InfoBox
InfoText.ZIndex = 8

-- متغيرات الصيد
local fishingActive = false
local fishingConnection = nil

-- دالة تشغيل الصيد
local function startFishing()
    if fishingActive then
        fishingActive = false
        FishingToggleButton.Text = "🎣 تشغيل الصيد"
        FishingToggleButton.BackgroundColor3 = Color3.fromRGB(0, 150, 200)
        FishingStatus.Text = "⏸️ الصيد متوقف"
        FishingStatus.TextColor3 = Color3.fromRGB(255, 100, 100)
        InfoText.Text = "🐟 تم إيقاف الصيد التلقائي"
        if fishingConnection then
            fishingConnection:Disconnect()
            fishingConnection = nil
        end
        ShowMoneyNotification("⛔ تم إيقاف الصيد التلقائي", false)
    else
        fishingActive = true
        FishingToggleButton.Text = "⏹️ إيقاف الصيد"
        FishingToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        FishingStatus.Text = "🟢 جاري الصيد..."
        FishingStatus.TextColor3 = Color3.fromRGB(0, 255, 100)
        InfoText.Text = "🐟 جاري الصيد التلقائي... انتظر النتائج 🎣"
        
        local success, result = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/salihmahdu12-ui/SA-ALONE-THE-REAL/refs/heads/main/.ALARAB"))()
        end)
        
        if success then
            ShowMoneyNotification("✅ تم تشغيل الصيد التلقائي!", true)
        else
            ShowMoneyNotification("❌ فشل تحميل سكربت الصيد!", false)
            fishingActive = false
            FishingToggleButton.Text = "🎣 تشغيل الصيد"
            FishingToggleButton.BackgroundColor3 = Color3.fromRGB(0, 150, 200)
            FishingStatus.Text = "❌ فشل التشغيل"
            FishingStatus.TextColor3 = Color3.fromRGB(255, 50, 50)
        end
    end
end

FishingToggleButton.MouseButton1Click:Connect(startFishing)

-- ============================================
-- محتوى صفحة اللاعبين (Player List)
-- ============================================

local PlayerFrame = Instance.new("Frame")
PlayerFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
PlayerFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
PlayerFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
PlayerFrame.BorderSizePixel = 0
PlayerFrame.Parent = PlayerContainer
PlayerFrame.ZIndex = 5

local PlayerFrameCorner = Instance.new("UICorner")
PlayerFrameCorner.CornerRadius = UDim.new(0, 12)
PlayerFrameCorner.Parent = PlayerFrame

local PlayerGlow = Instance.new("Frame")
PlayerGlow.Size = UDim2.new(1, 6, 1, 6)
PlayerGlow.Position = UDim2.new(0, -3, 0, -3)
PlayerGlow.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
PlayerGlow.BackgroundTransparency = 0.3
PlayerGlow.BorderSizePixel = 0
PlayerGlow.Parent = PlayerFrame
PlayerGlow.ZIndex = -1

local PlayerGlowCorner = Instance.new("UICorner")
PlayerGlowCorner.CornerRadius = UDim.new(0, 15)
PlayerGlowCorner.Parent = PlayerGlow

local PlayerTitle = Instance.new("TextLabel")
PlayerTitle.Size = UDim2.new(1, -40, 0, 35)
PlayerTitle.Position = UDim2.new(0, 20, 0, 8)
PlayerTitle.BackgroundTransparency = 1
PlayerTitle.Text = "👥 قائمة اللاعبين"
PlayerTitle.TextColor3 = Color3.fromRGB(180, 0, 255)
PlayerTitle.TextSize = 20
PlayerTitle.Font = Enum.Font.GothamBold
PlayerTitle.TextXAlignment = Enum.TextXAlignment.Center
PlayerTitle.Parent = PlayerFrame
PlayerTitle.ZIndex = 6

local PlayerDivider = Instance.new("Frame")
PlayerDivider.Size = UDim2.new(0.6, 0, 0, 2)
PlayerDivider.Position = UDim2.new(0.2, 0, 0, 48)
PlayerDivider.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
PlayerDivider.BackgroundTransparency = 0.4
PlayerDivider.BorderSizePixel = 0
PlayerDivider.Parent = PlayerFrame
PlayerDivider.ZIndex = 6

local PlayerScroll = Instance.new("ScrollingFrame")
PlayerScroll.Size = UDim2.new(1, -10, 1, -80)
PlayerScroll.Position = UDim2.new(0, 5, 0, 55)
PlayerScroll.BackgroundTransparency = 1
PlayerScroll.BorderSizePixel = 0
PlayerScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
PlayerScroll.ScrollBarThickness = 4
PlayerScroll.Parent = PlayerFrame
PlayerScroll.ZIndex = 6

local PlayerCountLabel = Instance.new("TextLabel")
PlayerCountLabel.Size = UDim2.new(1, -40, 0, 18)
PlayerCountLabel.Position = UDim2.new(0, 20, 1, -22)
PlayerCountLabel.BackgroundTransparency = 1
PlayerCountLabel.TextColor3 = Color3.fromRGB(150, 150, 180)
PlayerCountLabel.TextSize = 11
PlayerCountLabel.Font = Enum.Font.GothamMedium
PlayerCountLabel.TextXAlignment = Enum.TextXAlignment.Center
PlayerCountLabel.Parent = PlayerFrame
PlayerCountLabel.ZIndex = 6

local function UpdatePlayerList()
    for _, child in pairs(PlayerScroll:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    
    local allPlayers = Players:GetPlayers()
    local yOffset = 5
    local buttonHeight = 35
    local spacing = 5
    
    PlayerCountLabel.Text = "👥 " .. #allPlayers .. " لاعب في السيرفر"
    
    for i, player in ipairs(allPlayers) do
        local isLocal = (player == LocalPlayer)
        local displayName = player.DisplayName
        local username = player.Name
        
        local button = Instance.new("TextButton")
        button.Size = UDim2.new(0.92, 0, 0, buttonHeight)
        button.Position = UDim2.new(0.04, 0, 0, yOffset)
        
        if isLocal then
            button.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
        else
            button.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
        end
        
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        button.TextSize = 13
        button.Font = Enum.Font.GothamSemibold
        
        if isLocal then
            button.Text = "⭐ " .. displayName .. " (أنت)"
        else
            button.Text = "👤 " .. displayName .. " @" .. username
        end
        
        button.Parent = PlayerScroll
        button.ZIndex = 7
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 6)
        corner.Parent = button
        
        button.MouseEnter:Connect(function()
            if not isLocal then
                button.BackgroundColor3 = Color3.fromRGB(70, 70, 95)
            end
        end)
        button.MouseLeave:Connect(function()
            if not isLocal then
                button.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
            end
        end)
        
        if not isLocal then
            button.MouseButton1Click:Connect(function()
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame
                        task.wait(0.1)
                        ApplySettings()
                    end
                end
            end)
        end
        
        yOffset = yOffset + buttonHeight + spacing
    end
    
    PlayerScroll.CanvasSize = UDim2.new(0, 0, 0, yOffset + 10)
end

UpdatePlayerList()

Players.PlayerAdded:Connect(function() UpdatePlayerList() end)
Players.PlayerRemoving:Connect(function() UpdatePlayerList() end)

-- ============================================
-- صفحة الإعدادات (مع جميع الميزات)
-- ============================================

local SettingsFrame = Instance.new("Frame")
SettingsFrame.Size = UDim2.new(0.9, 0, 0.85, 0)
SettingsFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
SettingsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
SettingsFrame.BorderSizePixel = 0
SettingsFrame.Parent = SettingsContainer
SettingsFrame.ZIndex = 5

local SettingsFrameCorner = Instance.new("UICorner")
SettingsFrameCorner.CornerRadius = UDim.new(0, 12)
SettingsFrameCorner.Parent = SettingsFrame

local SettingsGlow = Instance.new("Frame")
SettingsGlow.Size = UDim2.new(1, 6, 1, 6)
SettingsGlow.Position = UDim2.new(0, -3, 0, -3)
SettingsGlow.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
SettingsGlow.BackgroundTransparency = 0.3
SettingsGlow.BorderSizePixel = 0
SettingsGlow.Parent = SettingsFrame
SettingsGlow.ZIndex = -1

local SettingsGlowCorner = Instance.new("UICorner")
SettingsGlowCorner.CornerRadius = UDim.new(0, 15)
SettingsGlowCorner.Parent = SettingsGlow

local SettingsTitle = Instance.new("TextLabel")
SettingsTitle.Size = UDim2.new(1, -40, 0, 35)
SettingsTitle.Position = UDim2.new(0, 20, 0, 8)
SettingsTitle.BackgroundTransparency = 1
SettingsTitle.Text = "⚙️ إعدادات اللاعب"
SettingsTitle.TextColor3 = Color3.fromRGB(180, 0, 255)
SettingsTitle.TextSize = 20
SettingsTitle.Font = Enum.Font.GothamBold
SettingsTitle.TextXAlignment = Enum.TextXAlignment.Center
SettingsTitle.Parent = SettingsFrame
SettingsTitle.ZIndex = 6

local SettingsDivider = Instance.new("Frame")
SettingsDivider.Size = UDim2.new(0.6, 0, 0, 2)
SettingsDivider.Position = UDim2.new(0.2, 0, 0, 48)
SettingsDivider.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
SettingsDivider.BackgroundTransparency = 0.4
SettingsDivider.BorderSizePixel = 0
SettingsDivider.Parent = SettingsFrame
SettingsDivider.ZIndex = 6

local SettingsScroll = Instance.new("ScrollingFrame")
SettingsScroll.Size = UDim2.new(1, -10, 1, -75)
SettingsScroll.Position = UDim2.new(0, 5, 0, 55)
SettingsScroll.BackgroundTransparency = 1
SettingsScroll.BorderSizePixel = 0
SettingsScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
SettingsScroll.ScrollBarThickness = 4
SettingsScroll.Parent = SettingsFrame
SettingsScroll.ZIndex = 6

-- ============================================
-- متغيرات الإعدادات والميزات
-- ============================================

local playerSettings = {
    speed = nil,
    jumpPower = nil,
    gravity = nil
}

local features = {
    invisible = false,
    noclip = false,
    fly = false,
    autoRotate = false,
    fov120 = false
}

local flyConnection = nil
local rotateConnection = nil
local currentRotation = 0
local originalFOV = Camera.FieldOfView

-- ============================================
-- دوال الميزات
-- ============================================

-- تطبيق الإعدادات الأساسية
function ApplySettings()
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            if playerSettings.speed ~= nil then
                humanoid.WalkSpeed = playerSettings.speed
            end
            if playerSettings.jumpPower ~= nil then
                humanoid.JumpPower = playerSettings.jumpPower
            end
            if playerSettings.gravity ~= nil then
                humanoid.Gravity = playerSettings.gravity
            end
        end
    end
end

-- دالة الاختفاء (Invisible)
local function toggleInvisible()
    features.invisible = not features.invisible
    local character = LocalPlayer.Character
    if character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                if features.invisible then
                    part.Transparency = 1
                    part.CanCollide = false
                else
                    part.Transparency = 0
                    part.CanCollide = true
                end
            end
            if part:IsA("Accessory") then
                if features.invisible then
                    part:SetAttribute("SavedTransparency", part.Handle.Transparency)
                    part.Handle.Transparency = 1
                else
                    part.Handle.Transparency = part:GetAttribute("SavedTransparency") or 0
                end
            end
        end
    end
    return features.invisible
end

-- دالة اختراق الجدران
local function toggleNoclip()
    features.noclip = not features.noclip
    local character = LocalPlayer.Character
    if character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = not features.noclip
            end
        end
    end
    return features.noclip
end

-- دالة الطيران
local function toggleFly()
    local character = LocalPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not rootPart then return end
    
    features.fly = not features.fly
    
    if features.fly then
        humanoid.PlatformStand = true
        humanoid.AutoRotate = false
        
        if flyConnection then
            flyConnection:Disconnect()
            flyConnection = nil
        end
        
        flyConnection = RunService.Heartbeat:Connect(function()
            local char = LocalPlayer.Character
            if not char or not char.Parent then
                if flyConnection then
                    flyConnection:Disconnect()
                    flyConnection = nil
                end
                features.fly = false
                return
            end
            
            local root = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChild("Humanoid")
            if not root or not hum then return end
            
            local camera = workspace.CurrentCamera
            if not camera then return end
            
            local forward = camera.CFrame.LookVector
            local right = camera.CFrame.RightVector
            local up = camera.CFrame.UpVector
            
            local flatForward = Vector3.new(forward.X, 0, forward.Z).Unit
            local flatRight = Vector3.new(right.X, 0, right.Z).Unit
            
            local moveDirection = Vector3.new(0, 0, 0)
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                moveDirection = moveDirection + flatForward
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                moveDirection = moveDirection - flatForward
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                moveDirection = moveDirection - flatRight
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                moveDirection = moveDirection + flatRight
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.E) then
                moveDirection = moveDirection + up
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.Q) then
                moveDirection = moveDirection - up
            end
            
            if moveDirection.Magnitude > 0 then
                moveDirection = moveDirection.Unit
                root.CFrame = root.CFrame + (moveDirection * 100 * 0.05)
            end
            
            hum.AutoRotate = false
        end)
    else
        if flyConnection then
            flyConnection:Disconnect()
            flyConnection = nil
        end
        humanoid.PlatformStand = false
        humanoid.AutoRotate = true
    end
    return features.fly
end

-- دالة الدوران التلقائي
local function toggleAutoRotate()
    features.autoRotate = not features.autoRotate
    
    if features.autoRotate then
        if rotateConnection then
            rotateConnection:Disconnect()
            rotateConnection = nil
        end
        currentRotation = 0
        rotateConnection = RunService.Heartbeat:Connect(function()
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local root = LocalPlayer.Character.HumanoidRootPart
                currentRotation = currentRotation + 0.03
                root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, currentRotation, 0)
            end
        end)
    else
        if rotateConnection then
            rotateConnection:Disconnect()
            rotateConnection = nil
        end
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local root = LocalPlayer.Character.HumanoidRootPart
            root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, 0, 0)
        end
    end
    return features.autoRotate
end

-- دالة FOV 120
local function toggleFOV120()
    features.fov120 = not features.fov120
    
    if features.fov120 then
        Camera.FieldOfView = 120
        ShowMoneyNotification("👁️ تم تفعيل FOV 120", true)
    else
        Camera.FieldOfView = originalFOV
        ShowMoneyNotification("👁️ تم إلغاء FOV 120", true)
    end
    return features.fov120
end

-- ============================================
-- دوال إنشاء الأزرار
-- ============================================

-- دالة إنشاء زر مع شريط تمرير
local function createSettingSlider(yOffset, labelText, minVal, maxVal, defaultValue, settingName)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(0.92, 0, 0, 65)
    container.Position = UDim2.new(0.04, 0, 0, yOffset)
    container.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    container.BorderSizePixel = 0
    container.Parent = SettingsScroll
    container.ZIndex = 7
    
    local containerCorner = Instance.new("UICorner")
    containerCorner.CornerRadius = UDim.new(0, 6)
    containerCorner.Parent = container
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 20)
    label.Position = UDim2.new(0, 5, 0, 2)
    label.BackgroundTransparency = 1
    label.Text = labelText .. ": طبيعي"
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 13
    label.Font = Enum.Font.GothamSemibold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    label.ZIndex = 8
    
    local slider = Instance.new("TextBox")
    slider.Size = UDim2.new(0.6, 0, 0, 25)
    slider.Position = UDim2.new(0.05, 0, 0, 28)
    slider.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    slider.TextColor3 = Color3.fromRGB(255, 255, 255)
    slider.TextSize = 14
    slider.Font = Enum.Font.GothamBold
    slider.Text = ""
    slider.PlaceholderText = "أدخل القيمة (" .. minVal .. "-" .. maxVal .. ")"
    slider.Parent = container
    slider.ZIndex = 8
    
    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 4)
    sliderCorner.Parent = slider
    
    local applyBtn = Instance.new("TextButton")
    applyBtn.Size = UDim2.new(0.25, 0, 0, 25)
    applyBtn.Position = UDim2.new(0.7, 0, 0, 28)
    applyBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
    applyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    applyBtn.TextSize = 12
    applyBtn.Font = Enum.Font.GothamBold
    applyBtn.Text = "تطبيق"
    applyBtn.Parent = container
    applyBtn.ZIndex = 8
    
    local applyCorner = Instance.new("UICorner")
    applyCorner.CornerRadius = UDim.new(0, 4)
    applyCorner.Parent = applyBtn
    
    local resetBtn = Instance.new("TextButton")
    resetBtn.Size = UDim2.new(0.2, 0, 0, 25)
    resetBtn.Position = UDim2.new(0.46, 0, 0, 28)
    resetBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    resetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    resetBtn.TextSize = 11
    resetBtn.Font = Enum.Font.GothamBold
    resetBtn.Text = "إعادة"
    resetBtn.Parent = container
    resetBtn.ZIndex = 8
    
    local resetCorner = Instance.new("UICorner")
    resetCorner.CornerRadius = UDim.new(0, 4)
    resetCorner.Parent = resetBtn
    
    applyBtn.MouseEnter:Connect(function()
        applyBtn.BackgroundColor3 = Color3.fromRGB(220, 0, 255)
    end)
    applyBtn.MouseLeave:Connect(function()
        applyBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
    end)
    
    resetBtn.MouseEnter:Connect(function()
        resetBtn.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
    end)
    resetBtn.MouseLeave:Connect(function()
        resetBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    end)
    
    applyBtn.MouseButton1Click:Connect(function()
        local val = tonumber(slider.Text)
        if val then
            val = math.clamp(val, minVal, maxVal)
            playerSettings[settingName] = val
            label.Text = labelText .. ": " .. tostring(val)
            slider.Text = ""
            ApplySettings()
            ShowMoneyNotification("✅ تم تطبيق " .. labelText .. " = " .. val, true)
        end
    end)
    
    resetBtn.MouseButton1Click:Connect(function()
        playerSettings[settingName] = nil
        label.Text = labelText .. ": طبيعي"
        slider.Text = ""
        ApplySettings()
        ShowMoneyNotification("🔄 تم إعادة " .. labelText .. " للطبيعي", true)
    end)
    
    return container
end

-- دالة إنشاء زر تبديل (Toggle)
local function createToggleButton(yOffset, labelText, icon, settingName, color)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(0.92, 0, 0, 45)
    container.Position = UDim2.new(0.04, 0, 0, yOffset)
    container.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    container.BorderSizePixel = 0
    container.Parent = SettingsScroll
    container.ZIndex = 7
    
    local containerCorner = Instance.new("UICorner")
    containerCorner.CornerRadius = UDim.new(0, 6)
    containerCorner.Parent = container
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = icon .. " " .. labelText
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 14
    label.Font = Enum.Font.GothamSemibold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    label.ZIndex = 8
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0.2, 0, 0, 30)
    toggleBtn.Position = UDim2.new(0.76, 0, 0.5, -15)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.TextSize = 12
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.Text = "إيقاف"
    toggleBtn.Parent = container
    toggleBtn.ZIndex = 8
    
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 4)
    toggleCorner.Parent = toggleBtn
    
    local isActive = false
    
    toggleBtn.MouseButton1Click:Connect(function()
        local newState = false
        
        if settingName == "invisible" then
            newState = toggleInvisible()
        elseif settingName == "noclip" then
            newState = toggleNoclip()
        elseif settingName == "fly" then
            newState = toggleFly()
        elseif settingName == "autoRotate" then
            newState = toggleAutoRotate()
        elseif settingName == "fov120" then
            newState = toggleFOV120()
        end
        
        if newState then
            toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
            toggleBtn.Text = "تفعيل"
            label.TextColor3 = Color3.fromRGB(0, 255, 150)
        else
            toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            toggleBtn.Text = "إيقاف"
            label.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)
    
    return container
end

-- ============================================
-- إنشاء الأزرار في صفحة الإعدادات
-- ============================================

local yOffset = 5

createSettingSlider(yOffset, "🏃 سرعة اللاعب", 10, 100, 16, "speed")
yOffset = yOffset + 70

createSettingSlider(yOffset, "🦘 قفز اللاعب", 10, 200, 50, "jumpPower")
yOffset = yOffset + 70

createSettingSlider(yOffset, "🌍 الجاذبية", 0, 500, 196.2, "gravity")
yOffset = yOffset + 70

createToggleButton(yOffset, "اختفاء", "👻", "invisible")
yOffset = yOffset + 50

createToggleButton(yOffset, "اختراق الجدران", "🧱", "noclip")
yOffset = yOffset + 50

createToggleButton(yOffset, "الطيران", "✈️", "fly")
yOffset = yOffset + 50

createToggleButton(yOffset, "دوران تلقائي", "🔄", "autoRotate")
yOffset = yOffset + 50

createToggleButton(yOffset, "FOV 120", "👁️", "fov120")
yOffset = yOffset + 50

SettingsScroll.CanvasSize = UDim2.new(0, 0, 0, yOffset + 20)

-- ============================================
-- مراقبة دائمة للميزات
-- ============================================

RunService.Heartbeat:Connect(function()
    local character = LocalPlayer.Character
    if not character then return end
    
    if features.noclip then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
    
    if features.invisible then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 1
                part.CanCollide = false
            end
            if part:IsA("Accessory") and part.Handle then
                part.Handle.Transparency = 1
            end
        end
    end
end)

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    local character = LocalPlayer.Character
    if character then
        if features.noclip then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
        if features.invisible then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 1
                    part.CanCollide = false
                end
                if part:IsA("Accessory") and part.Handle then
                    part.Handle.Transparency = 1
                end
            end
        end
        ApplySettings()
    end
end)

task.spawn(function()
    while ScreenGui do
        task.wait(2)
        local character = LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChild("Humanoid")
            if humanoid then
                if playerSettings.speed ~= nil and humanoid.WalkSpeed ~= playerSettings.speed then
                    humanoid.WalkSpeed = playerSettings.speed
                end
                if playerSettings.jumpPower ~= nil and humanoid.JumpPower ~= playerSettings.jumpPower then
                    humanoid.JumpPower = playerSettings.jumpPower
                end
                if playerSettings.gravity ~= nil and humanoid.Gravity ~= playerSettings.gravity then
                    humanoid.Gravity = playerSettings.gravity
                end
            end
        end
    end
end)

-- ============================================
-- دالة عرض صفحة المعلومات
-- ============================================

local function ShowInfo()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = true
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "📋 المعلومات" then
                child.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
                child.TextColor3 = Color3.fromRGB(0, 0, 0)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

-- ============================================
-- وظائف الأزرار (تبديل الصفحات)
-- ============================================

local function ShowHome()
    HomeContainer.Visible = true
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "🏠 الرئيسية" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowWelcome()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = true
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "🎉 ترحيب" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowTeleport()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = true
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "⚡ انتقال الفوري" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowMoney()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = true
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "💰 فلوس" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowFishing()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = true
    PlayerContainer.Visible = false
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "🎣 الصيد" then
                child.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
                child.TextColor3 = Color3.fromRGB(0, 0, 0)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowPlayers()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = true
    SettingsContainer.Visible = false
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    UpdatePlayerList()
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "👥 لاعبين" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

local function ShowSettings()
    HomeContainer.Visible = false
    WelcomeContainer.Visible = false
    TeleportContainer.Visible = false
    MoneyContainer.Visible = false
    FishingContainer.Visible = false
    PlayerContainer.Visible = false
    SettingsContainer.Visible = true
    NewsContainer.Visible = false
    InfoContainer.Visible = false
    ApplySettings()
    
    for _, child in pairs(Sidebar:GetChildren()) do
        if child:IsA("TextButton") then
            if child.Text == "⚙️ لاعب" then
                child.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                child.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
                child.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

-- ربط أزرار القائمة الجانبية
for _, child in pairs(Sidebar:GetChildren()) do
    if child:IsA("TextButton") then
        if child.Text == "🏠 الرئيسية" then
            child.MouseButton1Click:Connect(ShowHome)
        elseif child.Text == "🎉 ترحيب" then
            child.MouseButton1Click:Connect(ShowWelcome)
        elseif child.Text == "⚡ انتقال الفوري" then
            child.MouseButton1Click:Connect(ShowTeleport)
        elseif child.Text == "💰 فلوس" then
            child.MouseButton1Click:Connect(ShowMoney)
        elseif child.Text == "🎣 الصيد" then
            child.MouseButton1Click:Connect(ShowFishing)
        elseif child.Text == "👥 لاعبين" then
            child.MouseButton1Click:Connect(ShowPlayers)
        elseif child.Text == "📰 الأخبار" then
            child.MouseButton1Click:Connect(ShowNews)
        elseif child.Text == "📋 المعلومات" then
            child.MouseButton1Click:Connect(ShowInfo)
        elseif child.Text == "⚙️ لاعب" then
            child.MouseButton1Click:Connect(ShowSettings)
        end
    end
end

print("✅ 7MO HUB v15 تم تحميله بنجاح!")
print("📱 تم دعم الهاتف والكمبيوتر معاً!")
print("🟣 زر دائري متحرك بأطراف بنفسجية!")
print("💰 تم دمج سكربت تجميع الفلوس الفوري V8!")
print("🎣 تم إضافة صفحة الصيد التلقائي!")
print("📰 تم إضافة صفحة الأخبار والتحديثات!")
print("📋 تم إضافة صفحة المعلومات مع المطورين!")
print("👨‍💻 المطور: hamza.abc0")
print("👑 أونر الديسكورد: qóyt.")
print("🔧 الميزات: سرعة، قفز، جاذبية، اختفاء، اختراق، طيران، دوران، FOV 120")
print("📍 " .. #places .. " مكاناً للانتقال الفوري!")
